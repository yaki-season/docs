# 개발자 3 - 001 밸런스 데이터 파일과 로더·검증

- 문서 버전: `v1.1.0`
- 최종 변경일: `2026-07-23`
- 상태: `완료`
- 담당자: `개발자 3`

## 참조 spec

- `spec/data/DAT-001_콘텐츠와_밸런스_데이터_계약.md` - `DAT-001` - `v1.0.0`
- `spec/system/SYS-004_app_프로젝트와_소스코드_아키텍처.md` - `SYS-004` - `v1.0.0`

## 목표

`DAT-001` 계약에 따라 게임 수치를 담는 데이터 파일(JSON)과 그것을 읽어 검증하는 로더를 만든다.
조리 시간·손님 인내심·팁 배율 같은 수치가 코드 상수가 아니라 데이터에서 오고, 잘못된 값이 런타임
전에 검출되며, `candidate`/`approved` 상태가 구분되는 기반을 제공한다.

## 배경

수치가 지금은 코드 상수(`src/config/recipe.js`의 조리 임계값 등)로 흩어져 있어 기획자가 조절할 수
없다. 이 작업이 그 수치를 데이터로 옮겨 후속 게임 로직(`developer-1`)과 튜닝 도구(`developer-3/002`)의
공통 기반을 만든다.

## 범위

### 포함

- `DAT-001` 데이터 단위(메뉴·공정·품질·손님·영업일 등)의 JSON 파일 정의
- 레코드별 영구 식별자, 스키마 버전, 표시 키, 활성 여부, `candidate`/`approved` 상태
- 로드 전 참조·유형·범위·중복·순환 검증 (`DAT-001 §공통 규칙`)
- 단위 명시(비율 `0~1`, 시간 초, 재화 음수 아닌 정수 등)와 최소·최대 관계 검증
- 기존 코드 상수(조리 임계값 등)를 데이터로 이전하고 로직이 데이터를 읽도록 연결
- `approved`만 게임 기본값으로 로드하고 `candidate`는 격리하는 규칙

### 제외

- 편집 UI와 시뮬레이터 (`developer-3/002`)
- 영업일 루프·손님 로직 자체 (`developer-1`)
- 최종 밸런스 수치 결정 (기획자 몫, 값은 `candidate`로 채운다)
- 저장 데이터 마이그레이션의 런타임 구현

## 작업 절차

1. `DAT-001`의 데이터 단위를 JSON 스키마로 정의하고 파일 위치를 단일 원본으로 정한다.
2. 공통 규칙(식별자·버전·상태·활성·변경 이력)과 단위·범위·참조 검증을 구현한다.
3. 순환 참조·중복 식별자·미정의 참조·범위 위반을 로드 전에 검출한다.
4. `candidate`/`approved` 상태를 구분하고 `approved`만 기본값으로 로드하는 로더를 구현한다.
5. 기존 코드 상수(조리 임계값 등)를 데이터로 옮기고 값을 `candidate`로 표시한다.
6. 로더와 검증을 자동 테스트로 검증한다(정상 로드, 각 위반 케이스 거부).

## 의존성과 인계 조건

- 선행 작업: 없음 (아트·장면과 무관하게 착수 가능)
- 후속 작업: `developer-1/007`(영업일 루프 로직)과 `developer-3/002`(튜닝 도구)가 이 데이터·로더를 사용
- 다른 개발자에게 제공할 입력·출력: `developer-1`과 `developer-3/002`에 데이터 파일 경로·스키마,
  로더 API, 검증 규칙과 `candidate`/`approved` 접근 방법을 인계한다.

## 완료 기준

- [x] `DAT-001` 데이터 단위가 JSON 파일로 정의되고 단일 위치(`app/content/`)에 있다.
- [x] 미정의 참조·중복 식별자·범위 위반이 로드 전에 거부된다.
- [x] 단위와 최소·최대 관계(익힘 구간 순서)가 검증된다.
- [x] `candidate`/`approved` 상태가 구분되고 `approved`만 기본값으로 로드된다.
- [x] 기존 조리 임계값이 데이터로 이전되고 로직(`recipe.js`)이 부팅 시 데이터를 읽는다.
- [x] 로더·검증의 정상·위반 케이스가 자동 테스트로 검증된다.

## 구현 및 검증 결과

- app 구현 위치
  - `app/content/` — 데이터(processes·recipes·customers·campaign)와 `schema/` JSON Schema
  - `app/src/content/rules.js` — 교차 검증(참조·중복·구간 순서·상태). 브라우저·Node 공용
  - `app/src/content/validate.js` — Ajv 전체 스키마 검증. 개발·테스트 전용
  - `app/src/content/loader.js` — 런타임 로더(fetch + 가벼운 검사 + candidate/approved 분리)
  - `app/src/config/recipe.js` — `applyBalanceContent`로 조리 임계값을 데이터에서 읽음
  - `app/src/main.js` — 부팅 시 콘텐츠 로드·검증·반영(실패 시 안전 기본값)
  - `app/tests/unit/content.test.js` — 검증 11개
- 구현 기준 spec 버전: `DAT-001 v1.0.0`, `SYS-004 v1.0.0`
- 구현 기준 태스크 버전: `v1.0.0`
- 검증 방법: `npm test`(콘텐츠 검증 11개). 실제 `content/`를 스키마·교차 규칙으로 검증하고,
  범위 초과·미정의 참조·중복·구간 순서 위반의 거부와 `candidate` 격리, 데이터 구동을 확인.
  종단 테스트로 부팅 시 콘텐츠 로드가 404·콘솔 오류 없이 되고 게임이 정상임을 확인.
- 검증 결과: `2026-07-23` 기준 단위 81개(콘텐츠 11 추가), 종단 회귀 통과
- 설계 반영 사항
  - `SYS-004`대로 전체 스키마 검증(Ajv)은 테스트, 런타임은 가벼운 교차 검사로 분리했다.
  - `recipe.js`의 임계값 리터럴은 안전 기본값으로 남기고 데이터가 원본이 되게 했다. 로드 실패
    시 게임이 깨지지 않는다.
- 남은 위험
  - `DAT-001`이 `초안`이라 스키마 세부가 바뀔 수 있다. 값은 전부 `candidate`다.
  - 이번엔 조리·손님·영업일·레시피 단위만 구현했다. 성장·직원·시나리오 단위는 후속이다.
  - 손님 수치의 런타임 소비(인내심·간격)는 손님 시스템/영업일 루프(`developer-1/007`) 구현 시 연결된다.

## 변경 이력

| 이전 버전 | 새 버전 | 날짜 | 변경 유형 | 근거 spec 버전 | 변경 내용 | 재작업 영향 |
|---|---|---|---|---|---|---|
| 없음 | `v1.0.0` | `2026-07-23` | 최초 생성 | `DAT-001 v1.0.0`, `SYS-004 v1.0.0` | 밸런스 데이터 파일·로더·검증 작업 생성. 수치를 코드 상수에서 데이터로 이전하는 기반 | 없음 |
| `v1.0.0` | `v1.1.0` | `2026-07-23` | 구현 완료 | `DAT-001 v1.0.0`, `SYS-004 v1.0.0` | `content/` 데이터·스키마, Ajv 전체 검증·런타임 로더, 조리 임계값 데이터 이전 구현. 완료 기준 6개 충족, 상태를 `완료`로 변경 | 없음 |
