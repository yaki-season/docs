# 개발자 3 - 012 D1 첫 주문·공개 그릴 계약 네기마 2개·시작 2칸 정합

- 상태: `완료`
- 담당자: `개발자 3`
- 우선순위: `P1`
- 현재 milestone에 필요한 이유: D1 첫 손님 사람 PASS가 확정 규격(네기마 2개·시작 2칸 동시 조리)대로 구동되려면 데이터·계약이 먼저 정합돼야 한다.
- 하지 않으면 막히는 stage gate: D1 첫 손님 튜토리얼 정합(개발자 1 작업 011)과 D1 완전 구동

## 참조 spec

- `spec/data/DAT-001_콘텐츠와_밸런스_데이터_계약.md` - `DAT-001` - `v5.25.0`
- `spec/gameplay/GPL-004_조리_스테이션과_품질_서빙.md` - `GPL-004` - `v1.41.0`
- `spec/gameplay/GPL-005_경제_성장_직원과_보상.md` - `GPL-005` - `v2.23.0`

## 목표

D1 공개 첫 주문과 공개 그릴 runtime 계약을 `DAT-001 v5.25.0`의 확정값(첫 주문 네기마 2개,
`initialPlacementCount=2`, `initialPlacementSlots=[1,2]`, `timerStartPolicy=afterInitialBatchPlaced`,
그릴 시작 2칸·명성 해금 최대 8칸)으로 정합해, 개발자 1의 D1 슬라이스가 하드코딩 3/6 없이
데이터·계약만으로 구동되게 한다.

## 범위

### 포함

- `content/releases/d1-business-day-definition.v1.json`: `D1-ORDER-001`의 네기마 `quantity 3 → 2`,
  `totals.items 9 → 8`(customers/orders 불변) 재계산
- `content/campaign/day-d1.json`·`content/orders/early-campaign.json`의 `D1-ORDER-001` 정합
- `src/content/d1PublicRuntimeContract.js` `D1_PUBLIC_GRILL_BASELINE`:
  `slotCount 6 → 명성 해금 시작 2`, `slotUpgradeEnabled false → 명성 해금 가변`,
  `initialBatch.placementCount 3 → 2`, 검증 메시지 "첫 3개…" → "첫 2개…"
- 관련 schema(`content/schema/d1-release-definition.schema.json` 등)에서 개수·칸을 고정하는 제약만 정합
  (네기마 판매가 `basePriceG=3` 등 가격 상수는 변경하지 않는다)
- 정적 release artifact 재생성과 drift check
- 개발자 1 작업 011에 exact 계약 값 handoff

### 제외

- `src/d1-game.js`·`cookStations.js` 런타임 배선(개발자 1 작업 011)
- 아트·placeholder, 튜토리얼 힌트 UI 문구

## 작업 절차

1. `DAT-001 v5.25.0`의 D1 첫 주문·그릴 값을 정본으로 확인한다.
2. release definition·day-d1·early-campaign의 `D1-ORDER-001`을 네기마 2개로 정합하고 totals를 재계산한다.
3. `d1PublicRuntimeContract.js` baseline과 검증 메시지를 시작 2칸·batch 2로 정합한다.
4. 정상/오류 fixture와 계약 검증기를 갱신해 batch 2 동시 시작을 강제한다.
5. 정적 release artifact를 재생성하고 drift check와 전체 회귀를 통과시킨다.
6. 개발자 1 작업 011에 계약 값(placementCount 2, placementSlots [1,2], start slots 2)을 handoff한다.

## 완료 기준

- [x] `D1-ORDER-001` 네기마 `quantity=2`, `totals.items=8`이다.
- [x] `D1_PUBLIC_GRILL_BASELINE`이 시작 2칸·명성 해금·`initialBatch.placementCount=2`다.
- [x] batch 2가 아닌 fixture는 계약 검증에서 실패한다.
- [x] 정적 release artifact drift check와 전체 Vitest가 통과한다.
- [x] 개발자 1 작업 011이 소비할 exact 계약 값이 handoff된다.

## 의존성과 인계 조건

- 선행 작업: 없음(현재 확정 spec 소비)
- 생산자 → 소비자: 개발자 3 작업 012 → 개발자 1 작업 011
- 검증자: 통합 QA·릴리스 작업 001

## 구현 및 검증 결과

- 구현 위치: `app`(content/·src/content/·content/schema/)
- 구현 기준 spec 버전: 위 참조 spec
- 구현 기준 태스크 버전: `v1.0.1`
- 검증 방법: 계약 fixture·Ajv schema·release drift·전체 Vitest
- 현재 결과: app `20b8ee8` dirty 기준 네기마 2·총 8, 시작 2·최대 8·명성 해금, placement 2/[1,2]·두 번째 배치 뒤 동시 시작으로 정합했다. production customer sequence와 D1 대사도 2개로 맞췄고 batch 3 오류 fixture를 추가했다. release artifact SHA-256은 `fd4c9d9f3a4f189056da4dbfde940c7ad72604276137ca31a253e4d33422d073`, drift 일치, PM 재실행 전체 Vitest `47파일·358/358`, `git diff --check` 통과.
- 남은 위험: Developer 1 작업 011의 D1 슬라이스 가변 geometry·batch 2·분기형 순차 가이드 배선과 FHD/720 사람 gate
