# 개발자 1 - 005 2.5D 영업 장면과 손님 반응 구현

- 상태: `진행 중`
- 담당자: `개발자 1`

## 참조 spec

### 프로덕션 정본

- `spec/system/SYS-002_2.5D_스테이션_렌더링과_레이어_계약.md` - `SYS-002` - `v3.0.0`
- `spec/ui/UI-002_전체_게임_화면_입력_접근성.md` - `UI-002` - `v5.25.0`
- `spec/ui/UI-003_전체_게임_상세_화면_설계.md` - `UI-003` - `v1.2.0`
- `spec/gameplay/GPL-003_주문_손님_좌석과_러시_운영.md` - `GPL-003` - `v4.12.0`
- `spec/gameplay/GPL-004_조리_스테이션과_품질_서빙.md` - `GPL-004` - `v1.39.0`
- `spec/art/ART-002_전체_게임_비주얼_캐릭터_에셋.md` - `ART-002` - `v3.6.1`
- `spec/art/ART-003_런타임_아트_에셋_목록과_제작_계약.md` - `ART-003` - `v5.9.0`

### 단일 손님 테스트 화면 한정

- `spec/system/SYS-001_웹_프로토타입_런타임과_상태_관리.md` - `SYS-001` - `v1.0.0`
- `spec/scenario/SCN-001_인트로_제외_첫_손님_영업_장면.md` - `SCN-001` - `v2.0.0`
- `spec/ui/UI-001_웹_클릭_조리_및_서빙_인터페이스.md` - `UI-001` - `v2.0.0`
- `spec/gameplay/GPL-001_단일_야키토리_주문_수직_슬라이스.md` - `GPL-001` - `v1.0.0`
- `spec/art/ART-001_야키토리_프로토타입_픽셀_아트_에셋.md` - `ART-001` - `v1.1.0`
- `spec/qa/QA-001_최소_프로토타입_동작_검증.md` - `QA-001` - `v1.2.0`

## 목표

최신 기획의 **독립 손님 정보 화면·조립·그릴·드링크·서빙 화면**을 같은 플레이어 물리 위치와
공유 상태 위에서 전환할 수 있는 프로덕션 장면 구조로 정렬한다. 동시에 현 구현의 츠키오카 1인·네기마
1주문 장면은 폐기하지 않고 `SCN-001` 회귀·통합 테스트 화면으로 명시적으로 유지한다.

## 계약 분리와 우선순위

프로덕션 동작은 항상 `SYS-002 v3.0.0`, `UI-002 v5.25.0`, `UI-003 v1.2.0`, `GPL-003 v4.12.0`,
`GPL-004 v1.39.0`, `ART-002 v3.6.1`, `ART-003 v5.8.0`을 정본으로 한다. 이전 단일 수직 슬라이스 문서는 테스트 화면의 범위와
검증 절차에만 적용하며, 프로덕션 요구사항을 축소하거나 대체하지 않는다.

| 구분 | 프로덕션 장면 계약 | `SCN-001` 단일 손님 테스트 화면 계약 |
|---|---|---|
| 화면 구성 | 손님 정보, 조립, 그릴, 드링크, 서빙을 각각 독립 프리셋으로 구성한다. 조리 화면에는 주문 참조·공용 준비 목록만 남긴다. | 하나의 지속 카운터 장면에서 츠키오카와 한 주문의 조립·굽기·서빙을 검증한다. |
| 카메라·공간 | 모든 화면은 같은 플레이어 기준 위치·공간 앵커에서 파생한다. 공정 전환을 카메라의 좌우 평행 이동으로 표현하지 않는다. | 현재의 셰프측 고정 구도와 작업대 push-in은 회귀 검증을 위한 고정 연출로만 유지한다. |
| 손님·주문 | 좌석·그룹·주문 ID, 다중 동시 주문, 공용 준비 목록과 자동 연결을 지원할 인터페이스를 둔다. 상세 운영 로직은 작업 006의 책임이다. | `seat-03`의 츠키오카 1명, 네기마 1주문, 수동 주문 매트 및 `waiting→tasting→satisfied|neutral|retry`만 사용한다. |
| 아트 | 독립 화면용 승인 manifest와 `YS-HANDCRAFTED-NIGHT-v1` 아트를 사용해야 프로덕션 인수로 인정한다. | 더미 또는 회귀용 에셋을 허용한다. 테스트 결과는 프로덕션 아트 승인 증거가 아니다. |

## 범위

### 포함

- 기존 조립·굽기·서빙 판정과 상태 테스트 보존
- 하나의 렌더러 안에서 `SCR-SVC-CUSTOMERS`, `SCR-SVC-ASSEMBLY`, `SCR-SVC-GRILL`, `SCR-SVC-DRINK` 독립 프리셋을 등록·해제하고, 화면 전환 중 공정·주문·준비 상태를 보존하는 구조
- 같은 플레이어 위치에서 파생한 손님 정보·조립·그릴·드링크·서빙의 배경, 앵커, 가림선, HUD 안전 영역 계약
- 주문 ID→좌석/그룹→준비품 연결을 표시할 렌더 어댑터와 6석 앵커 인터페이스. 손님 운영 규칙의 활성화는 작업 006에서 수행
- 승인된 `ART-002/003 v3.1.0` manifest 및 `YS-HANDCRAFTED-NIGHT-v1` 에셋의 로딩·오류 처리·검증 경로
- `SCN-001` 단일 손님 테스트 화면의 고정된 입력 루프, 반응, 캡처, 성능 회귀 유지
- 화면별 레이어, 입력, 핫스팟, 접근성 및 빈 프레임 검증 추가

### 제외

- `GPL-003`의 대기 게이지, 그룹 입퇴장, 복수 주문, 정리와 손님 상태 머신 활성화 — 작업 006 책임
- 드링크의 실제 추출·제공 규칙, 품질 계산과 다수 주문 자동 서빙의 완성 구현 — 해당 공정/운영 작업의 책임
- 인트로, 외관, 재화·정산·저장·해금·다음 날 진행, 자유 카메라, glTF 리깅·물리·실시간 그림자

## 구현 기준선과 현재 구현 판정

1. `app@main`의 기존 단일 캔버스 구현(`src/index.html`, `src/main.js`, `src/render/sceneRenderer.js`,
   `src/config/sceneLayout.js`)은 **프로덕션 기본 화면이 아니라 `SCN-001` 테스트 화면**이다.
2. 해당 테스트 화면은 `CUSTOMER_SYSTEM_ENABLED=false`를 유지한다. 이 플래그를 켜거나 단일 장면을
   다중 손님 화면으로 확장하지 않는다.
3. 현재의 배경·손님 유지형 카메라 프리셋과 더미 슬롯은 테스트 전용이다. 프로덕션 프리셋은 독립 화면을
   활성화하고 동일한 플레이어 기준 공간에서 필요한 오브젝트만 보여야 한다.
4. 프로덕션용 최종 아트는 Artist 1 작업 000, Artist 2 작업 023, Artist 3 작업 025가
   단일 semanticOwner로 사용자 승인·finalizer 인계한 manifest와 anchor metadata만 사용한다. 더미 아트로
   테스트 화면을 계속 실행할 수 있으나, 이를 프로덕션 장면 완료로 보고하지 않는다.

## 작업 절차

1. 현재 단일 손님 구현을 `SCN-001` 테스트 모드로 표기하고, 테스트·캡처·성능 결과의 적용 범위를 분리한다.
2. `sceneLayout`과 렌더러에 `UI-003`의 프로덕션 화면 프리셋 레지스트리(`SCR-SVC-CUSTOMERS/ASSEMBLY/GRILL/DRINK`)를 추가한다. 서빙은 별도 주화면이 아니라 손님 화면의 공용 완성품 dock·좌석 anchor·`OVR-SERVE-QUANTITY` 연결로 제공한다.
   프리셋은 같은 플레이어 기준 anchor를 사용하고, 이전처럼 카메라 x 위치를 이동해 장면을 공유하지 않는다.
3. 화면별 배경·고정 오브젝트·핫스팟·HUD 안전 영역을 manifest ID와 metadata로 등록하고, 전환 시 필요한
   레이어만 활성화한다.
4. 좌석 6개와 주문/그룹 표식의 표시 인터페이스, 공용 준비 목록·부분 제공 상태의 렌더 어댑터를 만든다.
   실제 손님 배정·대기·정리·자동 제공 상태 전이는 작업 006과 `GPL-004`에서 연결한다.
5. 좌/우·빠른 화면 전환, 상태 보존, 웹 접근성 입력과 빈 프레임·가림·44px 핫스팟을 검증한다.
6. 승인 아트를 적용한 프로덕션 화면별 캡처·manifest·성능 검증을 추가한다. 승인 전에는 테스트 화면의
   더미 회귀만 실행하고 결과를 분리 기록한다.

## 권장 파일 단위 변경

| 경로 | 책임 |
|---|---|
| `app/src/index.html`, `app/src/main.js` | 현재 테스트 모드의 명시, 향후 프로덕션 화면 디렉터 부팅 경계 |
| `app/src/config/sceneLayout.js` | 테스트 레이아웃과 프로덕션 독립 화면 metadata의 분리 |
| `app/src/render/sceneRenderer.js` | 화면별 레이어 활성화·해제와 하나의 렌더 루프 |
| `app/src/render/stationDirector.js` | 독립 화면 전환, 상태 보존, 전환 애니메이션 정리 |
| `app/src/render/renderStateAdapter.js` | 주문·좌석·그룹·공용 준비 목록을 렌더 상태로 투영 |
| `app/src/render/hotspotManager.js` | 레이캐스트·키보드 공통 입력 및 최소 입력 영역 |
| `app/tests/e2e/scene.spec.js` | 테스트 화면 회귀와 프로덕션 화면 계약을 분리한 검증 |
| `app/art/review/developer-1-005/` | 단일 손님 회귀 캡처와 프로덕션 인수 캡처의 별도 보관 |

## 의존성과 인계 조건

- 단일 손님 테스트 화면은 현재 더미·회귀 에셋으로 계속 실행할 수 있으며 Artist 승인에 의해 중단되지 않는다.
- 프로덕션 화면 인수에는 Artist 1 작업 000·Artist 2 작업 023·Artist 3 작업 025의 개별 사용자
  승인·runtime handoff와 `ART-002/003`의
  독립 화면 manifest·anchor metadata가 필요하다.
- 작업 006은 이 작업이 **프로덕션** 손님 화면의 6석·주문 표식·공용 준비 목록 렌더 인터페이스를 제공한 뒤
  연결한다. 단일 손님 테스트 화면 완료는 작업 006의 선행 조건을 충족하지 않는다.
- 드링크의 실제 공정 완료와 최신 `GPL-004`의 자동 제공 계약은 작업 006의 다중 손님 활성화 전에 필요하다.

## 완료 기준

### 단일 손님 테스트 화면

- [x] 츠키오카 1명·네기마 1주문의 조립·양면 굽기·접시 전달·품질 반응 회귀가 동작한다.
- [x] 테스트 장면은 한 캔버스·한 렌더 루프와 정해진 캡처·자동 성능 예산을 유지한다.
- [x] 테스트 결과와 더미 에셋이 프로덕션 인수 증거가 아님을 app 문서와 태스크에 기록한다.

### 프로덕션 장면 구조

- [~] `SCR-SVC-CUSTOMERS/ASSEMBLY/GRILL/DRINK`가 독립 프리셋으로 전환된다(증분 1: 같은 PLAYER_EYE에서 파생한 화면별 시선 프리셋·화면별 오브젝트 활성). 서빙은 완성품을 그릴 화면에서 집어 손님 화면 serveMat으로 전달하는 화면 교차 흐름으로 동작. dock·좌석 anchor 정식화는 후속.
- [x] 화면 전환 중 주문·공정·준비품 상태가 보존되고, 급한 전환은 마지막 화면으로 수렴하며(§104), 전환 중 새 화면 조작은 잠긴다(§71).
- [~] 6석·주문 표식·공용 준비 목록을 작업 006과 `GPL-004`가 연결할 수 있다(증분 2: 6석 어댑터, 증분 3: 준비 목록, 증분 5: `customerOps` 손님 생애주기 운영 루프로 입장·주문·서빙·퇴장·정리를 실제 구동). 2인 그룹·부분 제공·러시는 후속.
- [ ] 승인 manifest와 `YS-HANDCRAFTED-NIGHT-v1` 아트가 모든 프로덕션 참조에서 로드된다.
- [ ] 각 프로덕션 화면의 레이어·가림·빈 프레임·성능·접근성 검증과 캡처가 기록된다.

## 구현 및 검증 결과

- 현재 app 구현은 `SCN-001` 테스트 화면이다. 단일 츠키오카, 네기마, 지속 카운터 구도, 수동 주문 매트와
  `waiting→tasting→satisfied|neutral|retry` 반응을 제공한다.
- 익힘 셰이더가 grill-skewer에 실제로 물려 있다(`src/render/grillMaterial.js`). 절차적 네기마 텍스처 위에
  마이야르·그을음·타레 광택·숯불 반사를 데이터 구동 익힘값으로 렌더하고, 키 G 실시간 튜너로 시각
  파라미터를 조정한다. 판정 시간은 `content/processes/grill.json`이 소유하며 셰이더는 결과만 그린다.
- 테스트 검증 결과: 단위 108 통과, e2e 36(두 기준 뷰포트) 통과. SwiftShader 부하로 `workers 1·retries 1`.
  자동 측정은 드로우콜 11·삼각형 20·DPR 1·평균 58.9 FPS다.
- 로컬 주의: `playwright.config`의 webServer는 `python3`이라 Windows 환경에서는 `python -m http.server 8777`을
  먼저 띄우고 `reuseExistingServer`로 재사용해야 한다(CI·Linux는 그대로 동작).
- **프로덕션 화면 디렉터 증분 1(2026-07-26):** 별도 진입점 `src/game.html`/`game.js`에 독립 영업 화면
  구조 뼈대를 구축했다. 같은 `PLAYER_EYE`에서 파생한 화면별 시선 프리셋(손님=정면·조립/그릴=아래·
  드링크=옆)으로 4개 화면을 전환하고(`src/config/screenLayout.js`, `src/render/productionRenderer.js`),
  전환 상태 머신(`src/render/stationDirector.js`)이 좌·우/퀵 이동·급한 전환 수렴(§104)·전환 중 조작
  잠금(§71)을 담당한다. 조립·그릴은 기존 `gameState`·익힘 셰이더를 재사용하고, 완성품을 그릴 화면에서
  집어 손님 화면 serveMat으로 전달하는 화면 교차 서빙이 동작한다. 조리 job은 화면과 독립 진행한다.
  공용 렌더 순수 함수는 `src/render/sceneMath.js`로 추출해 테스트 화면과 공유한다.
- 증분 1 검증: 단위 116(기존 108 + `stationDirector` 8), e2e 46(기존 36 + `production-screens` 5×2뷰포트).
  `SCN-001` 테스트 화면·기존 e2e 무변경.
- **프로덕션 화면 디렉터 증분 2(2026-07-26):** 손님 화면을 단일 더미 손님에서 **데이터 구동 6석
  렌더 어댑터**로 확장했다. `src/render/customerAdapter.js`의 순수 `buildSeatStates(점유[])`가 6석 렌더
  상태(점유·기분·주문·serve 대상)를 만들고, DOM 말풍선·대기 게이지·좌석 액터/serve 메시를 구동한다.
  이것이 작업 006/`GPL-003`이 실제 손님 운영을 꽂을 렌더 인터페이스다(현재는 seat-03 츠키오카 1명 데모).
  서빙은 좌석 대상(`seatServe:*`)으로 이뤄지며, 완성품을 그릴 화면에서 집어 손님 화면 좌석에 낸다.
- 증분 2 검증: 단위 120(+`customerAdapter` 4), e2e 52(+`production-customers` 3×2뷰포트).
- **프로덕션 화면 디렉터 증분 3(2026-07-26):** **공용 준비 목록(`preparedDock`)**을 추가해 조리(단일 job)와
  서빙(선반→좌석)을 분리했다. 그릴 완성 시 완성품이 선반에 쌓이고 조리는 다음 job으로 리셋되며, 손님
  화면에서 선반 완성품을 골라 좌석에 낸다. 좌석 반응은 완성품 품질 기반(`qualityFromCook`, gameState와
  동일 규칙). 이로써 여러 개를 미리 구워 쌓아뒀다 낼 수 있고 다중 주문의 토대가 된다(UI-003 §332).
- 증분 3 검증: 단위 122(+`preparedDock` 2), e2e 52(조리 루프를 조립→그릴→선반→좌석 서빙으로 갱신).
- **프로덕션 화면 디렉터 증분 4(2026-07-26):** D1 마지막 스테이션인 **생맥주**를 구현했다. `GPL-004`
  §34-46,49-2의 단일 레버 순차 따르기 — 레버 아래=맥주(적정 2.3~3.7초), 위=거품(0.3~1.7초), 총 4.7초
  넘으면 넘침 자동 차단. `src/render/drinkStation.js` 순수 상태 머신이 누적·넘침·품질(Perfect/Good/OK,
  넘침 제공=Fail)을 판정하고, 드링크 화면은 레버 press-hold(누르는 동안 흐름, 손 떼면 정지)·잔 채움 DOM
  패널(맥주·거품 layer·기준선)·완성/넘침 선택(낮은 품질 제공·폐기)을 제공한다. 완성 잔은 선반으로 간다.
  `preparedDock` 항목을 `{menu,label,good}`로 일반화해 꼬치·드링크 품질이 공존한다. 이로써 4개 D1 화면이
  모두 상호작용한다.
- 증분 4 검증: 단위 128(+`drinkStation` 6), e2e 62(+`production-drink` 5×2뷰포트).
- **프로덕션 화면 디렉터 증분 5(2026-07-26):** 6석 어댑터·준비 목록 위에 **실제 영업 손님 루프**를 얹었다
  (`GPL-003`). `src/render/customerOps.js` 순수 상태 머신이 좌석 생애주기(입장→고민 4-6초 `...`→주문서→
  수령 대기·인내심 감소→서빙(메뉴 매칭)→식사 15초→퇴장→정리 3초 홀드→빈자리→다음 손님)를 굴리고,
  콘텐츠(`customers/types.json` 유형·인내심, `campaign/day-d1.json` 입장 30초·고민·식사)로 구동한다.
  `customerAdapter`가 phase별로 렌더하고, 좌석 클릭이 주문 접수/서빙/정리 홀드를 phase로 분기한다.
  인내심 초과 시 화난 퇴장, 주문과 다른 메뉴는 거부. 단일 손님만이며 2인 그룹·재주문·러시·대기 회복은 후속.
- 증분 5 검증: 단위 134(+`customerOps` 6), e2e 66(+`production-customers` 6: 생애주기·타임아웃·메뉴불일치·정리홀드).
- **프로덕션 화면 디렉터 증분 6(2026-07-26):** 영업 종료→정산→다음 날로 **한 영업일을 닫는다**(GPL-002/005).
  `customerOps`가 서빙/이탈 결과(품질·대기·팁배수)를 기록하고, `src/render/daySettlement.js` 순수 계산이
  방문·제공·이탈·품질 분포·매출·팁·합계·평균 만족을 낸다(판매가·팁은 `businessDay` `revenueFor`/`tipFor`
  재사용으로 시뮬레이터·게임 단일 원본). `영업 종료` 버튼→정산 오버레이(손님 시간 정지), `다음 날`→리셋·재개.
- 증분 6 검증: 단위 139(+`daySettlement` 5), e2e 68(+`production-settlement` 1×2뷰포트).
- **프로덕션 화면 디렉터 증분 7(2026-07-26):** 손님 운영을 `GPL-003` 심화로 확장했다. **재주문**(식사 후
  만족도별 확률 100%→0.4·70~99%→0.2·미만→0으로 `orderSequence` 다음 항목 주문, §27,126), **2인 그룹**
  (인접 2좌석에만 입장 §10,11, 멤버별 상태, 한 명 인내심 초과 시 동행 즉시 공동 퇴장 §29 / 둘 다 완료 시
  함께 퇴장 §80), **러시**(파동식 입장 `waveSize`·활성 좌석 상한 `maxActive` §41). 시드 RNG로 재현 가능(§176).
- 증분 7 검증: 단위 142(`customerOps` 9로 확대), e2e 76(+`production-groups` 4: 그룹 입장·공동 퇴장·재주문·러시).
- **프로덕션 화면 디렉터 증분 8(2026-07-26):** 정산 재화로 업그레이드를 사며 **여러 날을 이어가는 성장·
  구매**를 추가했다(`GPL-005`). `src/render/progression.js` 순수 로직이 명성 델타(좋음 +3·이탈 -1 §2-2),
  구매 카탈로그 상태(보유/구매가능/명성잠금/선행잠금/골드부족), 골드 차감 구매, 소유 효과 집계
  (`basePriceMult`→economy)를 다룬다. 지갑(골드·명성)이 여러 날 지속되고, 영업 종료 시 총매출·명성이
  누적되며 소유 효과가 다음 날 판매가에 반영된다. 구매 화면(`SCR-META-PURCHASE`)은 지갑·카테고리 탭·
  아이템 카드·구매·게이팅을 제공한다. `grillSlots`·`seatCap`·직원 효과의 게임플레이 배선은 후속.
- 증분 8 검증: 단위 154(+`progression` 12), e2e 84(+`production-growth` 3: 정산 누적·구매 차감/효과·게이팅).
- **프로덕션 화면 디렉터 증분 9(2026-07-30):** 조리를 **멀티 잡**으로 전환했다(`GPL-004 §18-2`). 단일
  `gameState` 대신 `src/render/cookStations.js` 순수 모델이 **조립과 그릴을 분리**한다 — 조립은 그릴 상태와
  무관하게 언제든 진행되고 완성분은 대기 트레이로 쌓이며, 그릴은 N개의 칸이 각자 앞·뒤 독립 타이머로
  굽는다. 대기 트레이의 꼬치를 빈 칸에 올리고 칸을 눌러 뒤집기·회수하며, 익힘 판정은 `recipe.js`를
  재사용해 `SCN-001` 테스트 화면과 동일 규칙(8/16/21초)을 쓴다. `grillSlots` 업그레이드(증분 8에서 후속으로
  남긴 배선)를 `setSlots`로 연결해 구매 시 그릴 칸이 늘고 동시에 여러 꼬치를 굽는다. 첫 꼬치 이후 새
  꼬치가 이전 익힘 셰이더를 물려받던 버그를 고쳐, 굽지 않는 칸은 셰이더 익힘을 날것(0)으로 리셋한다.
- 증분 9 검증: 단위 162(+`cookStations` 8), e2e 88(+multi-job: 굽는 중 조립·칸 상태 보존·날것 리셋·그릴 칸
  업그레이드 2칸). 손님 인내심 밸런스 튜닝(office 100·blogger 80·couple 120초) 반영으로 그룹 퇴장 e2e 임계값 정정.
- **D1 단일 손님 2.5D 아트 통합(2026-07-30):** 승인된 D1 아트(`manifest.json` core 팩 — 배경
  `ARTIST-010-BACKGROUND-COMPLETE`·카운터 `BG-SERVICE-TABLE-ARTIST009`·츠키오카 대기/부분/식사 레이어·주문
  패널 스킨·네기마/생맥주 아이콘)를 **2.5D 진입점 `src/d1-scene.html`**에 물렸다. 세 레이어를 같은 셰프
  시점 카메라의 서로 다른 z(배경 -9·손님 -6·카운터 -3.5) 풀프레임 빌보드로 합성하고, 카메라를 방향 고정
  채 가로로 미세 이동시켜 레이어별 **시차(parallax)**를 만든다(손님이 배경에서 떠 보임). 손님 텍스처는
  D1 세션 phase(대기→부분 제공→반응)에 따라 교체된다. 세션·컨트롤 로직(`state/d1/session.js`)은 기존
  평면 DOM 진입점 `d1.html`과 공유하도록 `src/d1/view.js`로 추출(무회귀). 컨트롤 열은 좌측 도킹해 중앙
  손님이 보이게 했다. 로컬 개발 서버(`python http.server`)에서 매니페스트의 `/assets` URL이 `public/assets`로
  해소되도록 정션(gitignore)으로 연결.
- D1 2.5D 검증: 단위 162, e2e 90(+`d1-scene` 2: 3레이어 합성·깊이 순서·phase별 손님 텍스처 교체). 기존
  `d1-order`(평면 DOM) 무회귀.
- **D1 단일 손님 2.5D 플레이(2026-07-30):** 아트 뷰어(d1-scene)가 DOM 버튼만 눌러 진행되던 것을 넘어,
  `game.html`과 같은 조작 모델의 **플레이 진입점 `src/d1-game.html`**을 추가했다. 스테이션 좌·우/퀵/키보드
  전환, 화면 오브젝트 **레이캐스트 클릭**, 실시간 조리로 단일 손님(츠키오카)을 접수→조리→서빙→반응까지
  플레이한다. 손님 화면은 승인 D1 아트(배경·손님·카운터)를 깊이 이미지 레이어로, **아직 아트가 없는
  조립·그릴·드링크는 더미** 도형으로 구성한다("아트 있으면 에셋, 없으면 더미"). 조리·서빙 로직은 6석
  게임과 동일한 순수 모듈(`cookStations`·`drinkStation`·`preparedDock`·`stationDirector`)을 재사용하고,
  단일 손님 주문(생맥주 1·네기마 3) 컨트롤러만 새로 얹었다. 회귀 방지를 위해 렌더러는 별도 모듈
  (`d1SceneRenderer`, 이미지 레이어+더미+익힘 셰이더 지원)로 분리하고 `game.html`은 무변경. 자산
  파이프라인 계약(런타임 URL `/assets/…` → `public/assets`)에 맞춰 로컬 서버용 링크 헬퍼(`npm run
  link:assets`, `predev`/`pretest:e2e` 훅)를 추가.
- D1 2.5D 플레이 검증: 단위 162, e2e 96(+`d1-game` 3: 아트 레이어 로드·스테이션 전환·조립/그릴 화면
  클릭 조리→서빙→완료). `game.html`(6석)·`d1-scene`·`d1-order` 무회귀.
- **game.html(6석) 손님 화면 아트 적용(2026-07-30):** 사용자 요청으로 6석 프로덕션 게임의 손님 화면
  더미를 승인 D1 아트로 교체("아트 있으면 에셋, 없으면 더미, 위치 어색해도 무방"). 배경(dummy 벽)→
  `background-complete`, 카운터(dummy)→`service-table`를 풀프레임 이미지 레이어로, 6석 좌석 손님(dummy
  기분색 박스)→츠키오카 텍스처(풀프레임 아트에서 인물 bbox만 UV 크롭, 기분 틴트 제거, phase별
  대기/식사 교체)로 바꿨다. 조립·그릴·드링크는 아트 미보유로 더미 유지. `productionRenderer`에 이미지
  레이어(depthTest off·painter 순서)·좌석 텍스처/UV 크롭 지원을 추가하되 기존 더미 경로는 무변경.
  더불어 좌석 손님이 매 프레임 재표시돼 다른 화면 카메라에 걸쳐 보이던 잠재 버그를 손님 화면에서만
  보이도록 수정.
- game.html 아트 검증: 단위 162, e2e 98(+`production-art` 1: 좌석 손님 아트·화면별 가시성). 기존 프로덕션
  e2e 전부 무회귀.
- **손님 운영 심화 증분 10 — 서빙 품질·실패 반응(2026-07-30):** 서빙 결과를 3단계로 구분한다(`GPL-003`).
  **good**(만족·명성+), **ok**(과다 등 낮은 품질이지만 먹음·중립), **fail**(넘친 맥주 등 실패 품질) →
  손님이 화나서 **즉시 퇴장**(이탈 기록·명성-, 그룹이면 동반 퇴장). 또 **오배달**(주문과 다른 메뉴 제공
  시도) 시 인내심을 깎아(기본 5초) 반복 오배달이 이탈로 이어지게 했다. `customerOps.serve`가 품질 tier와
  퇴장 여부를 반환하고, game.js가 힌트로 피드백한다. 조리·드링크 산출물의 기존 라벨(좋음/과다/Perfect/
  Good/OK/Fail)을 그대로 tier에 매핑해 산출 계약은 무변경.
- 증분 10 검증: 단위 165(+`customerOps` 3: Fail 즉시 퇴장·낮은 품질 중립·오배달 인내심 패널티), e2e 100
  (+`production-customers` 1: Fail 품질 즉시 퇴장). 기존 프로덕션 e2e 무회귀.
- **손님 운영 심화 증분 11 — 부분 서빙(2026-07-30):** 한 주문이 **여러 개 수량**을 요구할 수 있고, 한 개씩
  내면 전량이 채워질 때까지 **수령 대기(부분 제공)**하다 다 채우면 식사한다(`GPL-003`). `orderSequence`의
  연속 같은 메뉴 토큰을 수량 라인으로 묶어(예: office `[drink,drink,skewer,skewer]` → 생맥주×2·네기마×2)
  콘텐츠 스키마 변경 없이 구현. `serve`가 부분/전량을 구분해 반환(부분이면 인내심 유지·계속 대기),
  뷰·말풍선에 진행도(`네기마 1/2`)를 표시한다. 재주문은 라인 단위로 다음 항목으로 넘어간다. 단일 수량
  라인은 기존과 동일하게 동작(무회귀).
- 증분 11 검증: 단위 166(+`customerOps` 1: 수량 전량 충족 후 식사), e2e 102(+`production-customers` 1:
  생맥주×2 부분 서빙). 기존 프로덕션 e2e 무회귀.
- **손님 운영 심화 증분 12 — 대기 회복·긴급 표시(2026-07-30):** 콘텐츠(day-d1 `waitRecoverySec`·
  `urgentThresholdSec`)를 배선했다. **대기 회복**: 부분 서빙으로 항목을 하나 낼 때마다 남은 인내심을
  회복(기본 10초, 전량 인내심 초과 금지)해 다음 항목을 낼 여유를 준다(`DAT-001 §14-1-1`). **긴급 표시**:
  남은 인내심이 임계(기본 15초) 이하면 뷰에 `urgent`를 실어 좌석 게이지·말풍선을 붉게·깜빡여 경고한다
  (대기 게이지 심화). 부분 서빙(증분 11)과 맞물려 다중 수량 주문의 긴장·완급을 만든다.
- 증분 12 검증: 단위 168(+`customerOps` 2: 부분 서빙 인내심 회복·긴급 임계), e2e 104(+`production-customers`
  1: 긴급 표시). 기존 프로덕션 e2e 무회귀.
- **증분 13 — 그릴 꼬치 익힘 상태 UI(2026-07-30):** 기획(`inbox/1.design/2026-07-30_그릴_꼬치_익힘_상태_UI_
  설계_초안.md`)에 따라 그릴 화면에서 굽는 각 칸 위에 **익힘 게이지 오버레이**를 띄운다. 셰이더 색만으로는
  읽기 어렵던 익힘을, **면(앞/뒤)·단계(덜 익음·적정·과다·탄)·적정 구간까지의 진행 마커·행동 힌트
  (뒤집기/회수)**로 명시한다. 게이지 구간·색은 `recipe.COOK_THRESHOLDS_SEC`(8/16/21초, 콘텐츠 구동)에서
  파생하고, `slotViews`(status·doneness·faceElapsedSec) 외 새 상태는 없다. 빈 칸·다른 화면에선 숨는다.
- 증분 13 검증: 단위 168, e2e 106(+`production-grill-gauge` 1: 면·단계·행동 힌트·화면별 가시성). 기존
  프로덕션 e2e 무회귀.
- **증분 14 — D1 프로덕션 고정 6칸 전환(2026-07-30):** `src/game.js`의 구형 1칸·업그레이드 확장과
  `src/d1-game.js`의 구형 2칸을 모두 `createD1CookStations()`로 교체했다. 본편은 처음부터 끝까지
  6칸이며 `grillSlots` 카탈로그 항목을 D1 구매 화면·소비 경로에서 제외한다. 첫 네기마 3개는 1·2번
  배치까지 `staged/contactFace=null/양면 0초`를 유지하고, 세 번째 배치의 같은 `now`로 1~3번 앞면
  접촉을 동시에 시작한다. 공중 뒤집기·그릴 밖 정지와 접촉면 한 면만 진행하는 순수 도메인 계약은 유지했다.
  `src/config/d1GrillLayout.js`가 두 프로덕션 레이아웃의 `grillSlot0~5` visual rect·실제 raycast hit target,
  대기 tray와 `grill.finished` 완료 tray anchor/bounds를 단일 원본으로 제공한다. `SCN-001`의
  `src/main.js`·`sceneLayout.js`는 변경하지 않았다.
- 증분 14 검증: 관련 단위 `16/16`, D1/프로덕션/성장 화면 FHD·720 E2E `28/28`, 고정 6칸 게이지
  FHD·720 `2/2`, 전체 Vitest `30파일·237/237` 통과. 첫 전체 `verify`는 자산 8개·참조 이미지 36장·
  Vitest 237개·Playwright 129개 통과, 2개 스킵 후 범위 밖 `SCN-001` 셰이더 튜너 미리보기 2개와
  실시간 부하성 4개가 실패했다. 드링크·성능은 격리 재실행에서 통과했고 튜너 2개만 두 해상도에서
  재현됐다.
- **증분 15 — 승인 master R3 그릴 좌표 정합(2026-07-30):** 반려된 빈 화면 재배치 후보를 사용하지
  않고 `CM-GRILL-STATION-QUEUED-SELECTION R3`를 시각 정본으로 고정했다.
  `d1GrillLayout.js`의 완료 tray를 FHD `{x:1534,y:130,width:218,height:342}`,
  anchor `{x:1643,y:301}`에서 분수 정규화하고 `visualRect`·`reservedBounds`와 독립 `hitRect`를
  공개한다. `productionRenderer`·`d1SceneRenderer`는 승인 위치의 visual mesh와 투명 raycast mesh를
  분리 소비하므로 이후 hit 확장이 visual을 변형하지 않는다. 현 tray는 이미 218×342px라 hit bounds를
  visual과 같게 유지했다. 기존 여섯 slot visual/hit은 R3 연속 석쇠 runtime safe bounds 안에 있음을
  FHD/720 master overlay로 확인했다. background·grate·finished tray·food overlay 책임도 별도 계약으로
  노출했으며 새 아트·manifest는 등록하지 않았다.
- 증분 15 검증: 레이아웃·조리 단위 `18/18`, 전체 Vitest 기준선 `33파일·253/253`, 실제
  조리→첫 뒤집기→양면 각 8초 적정→회수→완료 tray 클릭 FHD/720 `2/2`, master overlay FHD/720
  `2/2` 통과. 저장 payload에는 layout/rect/anchor가 없음을 확인해 schema와 migration은 변경하지 않았다.
  Developer 3 콘텐츠 완료 뒤 최종 `verify`는 자산 8개·reference 36장·Vitest `34파일·259/259`,
  Playwright `156개` 통과·`2개` 스킵·`8개` 실패다. 이번 D1 조리·tray·overlay는 두 해상도 모두
  통과했고, 실패는 기존 `SCN-001` 튜너 2건과 신규 밸런스(입장 30초·가격 3)를 구값
  (12초·100·정산 250)으로 기대하는 production groups/growth/settlement 6건으로 이 증분과 독립이다.
- **증분 16 — 결정적 데이터 회귀 정정(2026-07-30):** `SCN-001` grill tuner가
  `previewStage(0)`을 유효한 날것 override로 즉시 uniform에 반영하고 다음 animation frame에도
  `uDoneness=0`을 유지하도록 setter 흐름을 정정했다. 프로덕션 영업 화면은 실제 fetch한
  `/content/campaign/day-d1.json`·`progression/upgrades.json` 원본과 runtime 적용값을
  `contentContract()` 하나로 노출한다. rush·growth·settlement E2E는 이 계약의
  `spawnIntervalSec=30`, `basePrice=3`, 품질 배수·업그레이드 비용/효과를 읽어 기대값을 계산하며
  구형 12초·100G·250G 상수를 제거했다.
- 증분 16 검증: 관련 progression·settlement·customerOps 단위 `32/32`, 지정 Playwright 4파일은
  1280×720 `13/13`, 1920×1080 `13/13`, 합계 `26/26` 통과. `npm test`는 이번 변경과 무관한
  병행 runtime inventory의 fully-qualified
  semanticOwner 전환과 기존 테스트 불일치 3건으로 `261/264`이며, 수정 금지 대상
  `runtimeAssetResolver`·Artist inventory를 변경하지 않고 독립 실패로 분리했다.
- **증분 17 — 손님 배치·직접 클릭·그릴 뒤집기 시각 피드백 정정(2026-07-30):**
  풀프레임 손님 텍스처를 상체 UV로 자르고 actor를 카운터 뒤 좌석 높이에 배치해 하체가 테이블 앞으로
  분리되어 보이지 않게 했다. 주문·서빙·정리 raycast는 보이는 갈색 테이블 사각형을 제거하고 손님
  상체를 덮는 별도 완전 투명 hit mesh로 전환했다. 그릴은 기존 `0.3초 contactFace=null` 상태 머신과
  양면 각 8초 계약을 유지하면서 slot view의 `0→π` 회전 진행률을 실제 mesh quaternion에 적용하고
  뒤집힌 면도 보이도록 double-sided shader를 사용한다. 첫 3개 배치 완료와 앞면 8초 뒤 직접 클릭,
  0.3초 회전, 뒷면 8초 뒤 회수 순서를 화면 힌트로 명시했다.
- 증분 17 검증: 관련 단위 `22/22`, `d1-game` 실제 손님 actor 중심 클릭·투명 target·π 회전
  FHD/720 `6/6`, 전체 영업·저장 복구·R6 tray/slot·프로덕션 조리/서빙 회귀 FHD/720 `22/22`,
  전체 Vitest `42파일·300/300`, 담당 변경 `git diff --check` 통과.
- **증분 18 — 최소 시간 없는 반복 뒤집기·최종 누적 판정(2026-07-30):**
  `beginFlip`과 `clickSlot`의 적정 도달 차단을 제거했다. active slot은 공중 회전·재접촉 입력 잠금
  외에는 under에서도 반대 면으로 뒤집을 수 있고, 한 면이라도 under인 동안 클릭은 계속 뒤집기다.
  뒤집을 때 중간 `frontResult/backResult`를 고정하지 않으며 양면이 모두 under를 벗어난 뒤의 클릭에서
  최종 `frontElapsedSec/backElapsedSec`를 다시 분류해 회수 품질을 계산한다. 기존 0.3초 공중 정지,
  재접촉 입력 잠금, 접촉면 단독 진행, offGrill·snapshot 복구는 유지했다.
- 증분 18 검증: Developer 1 조리·영업 단위/통합 `28/28`, Developer 3 작업 011 독립 계약 `4/4`,
  전체 Vitest `43파일·308/308`, Chromium FHD/720 전체 영업·복구·프로덕션 조리/서빙 `18/18`.
  앞면 `3+5초`, 뒷면 `4+4초` 누적 후 최종 `Perfect`와 조기 반복 뒤집기·중복 입력 잠금·복구를 확인했다.
- **증분 19 — 승인 raw R3 footprint runtime 정합(2026-07-30):**
  Artist 1의 사용자 승인 `MDL-NEGIMA-GRILL-RAW` station consumption R3를
  `src/config/d1GrillLayout.js`의 screen-space 단일 원본으로 반영했다. slot0 visual은 FHD
  `{x:545,y:257,width:131,height:516}`, anchor `{x:609.6,y:515}`이며, 기존 hit 중심 간
  `153.6px` 간격을 그대로 사용해 slot0~5에 같은 Y·크기로 배치했다. raycast hitRect는 입력 회귀가
  없어 기존 FHD `124.8×324px`을 유지하고 visual과 별도 객체로 소비한다. `raw/cooking/flipped/
  proper/burnt`는 슬롯별 같은 `visualRect`·anchor·source model transform을 공유한다.
  마지막 visual 우측은 FHD `1444px`로 safe bounds `1460px` 안에 16px 남고 R6 tray와 90px,
  인접 visual끼리는 22.6px 떨어진다. 승인 모델은 `runtimeRegistrationAllowed=false`를 유지해
  manifest·finalizer·아트 파일은 변경하지 않았다.
- 증분 19 검증: 레이아웃 단위 `9/9`, 조리·선택 손님 제공 단위/통합 `29/29`, 전체 Vitest
  `43파일·311/311` 통과. 실제 프로덕션 visual/raycast mesh와 master overlay FHD/720 `4/4`,
  조기 뒤집기·0.3초 잠금·선택 손님 제공·조리→양면 8초→회수 FHD/720 `12/12`를 통과했다.
  슬롯 hit 중심·좌우 외곽의 `elementFromPoint`는 모두 canvas였고, slot5→tray 간격은 720p
  `60px`다. 생성 overlay는 Playwright 결과에 남기며 승인 review 산출물로 승격하지 않는다.
- **증분 20 — 그릴 다음 행동 도메인 projection(2026-07-31):**
  `src/render/cookStations.js`의 `slotViews()`에
  `nextAction='none'|'wait'|'flip'|'retrieve'`를 추가했다. 빈 칸, staged, 공중 회전·입력 잠금,
  한 면 이상 under, 양면 non-under의 판정을 `clickSlot()`과 같은 함수에서 계산하므로 UI가
  `contactFace`로 뒤집기/회수를 추측할 필요가 없다. 입력 잠금은 공중 회전 0.3초와 같은 구간으로
  정합화해 앞 3초 뒤 정확한 회전 종료 시점의 뒷면 0초 상태가 다시 `flip`을 제공한다.
  view projection은 snapshot에 저장하지 않으며 양면 누적·접촉면·저장 schema를 변경하지 않았다.
- 증분 20 검증: `cookStations` 단위 `16/16`, Developer 3 작업 011 계약을 함께 실행한
  `21/21`, 전체 Vitest `43파일·316/316` 통과. Developer 2 작업 011에는 exact 필드 계약,
  Artist 3 작업 025에는 기존 좌석 actor bounds·하단 중앙 anchor·유효 occlusion line을 서로
  분리된 versioned dispatch로 전달했다.
- **증분 21 — D1 이름 없는 엑스트라 actor 제작 계약(2026-07-31):**
  `src/render/d1ExtraActorContract.js` `v1.1.0`에서 현재 D1 customer phase를
  `considering/order-ready/waiting/urgent/receiving/eating/drinking/checkout/angry/leave/hidden`으로
  명시 매핑했다. ART-003의 16개 clip 납품 범위는 유지하며 현재 runtime이 선택하지 않는
  `enter/tasting/satisfied/disappointed/mismatch/retry`를 coverage-only로 분리했다. 여섯 좌석의
  FHD/720 visual bounds·lower-centre pivot, actor `z=-6`, occlusion line FHD `594`/720 `396`,
  `custCounter` foreground alpha와 별도 투명 `seatServe` hit target 소유를 고정했다. 아키·츠키오카
  외에는 이름·전기·관계·A/B 고정 정체성·FIFO/주문 번호를 갖지 않는 엑스트라로 제한한다.
  generic `computeSeats(6)`와 기존 handoff 수치 drift는 runtime 미바인딩 상태로 명시하고 이번 증분에서
  렌더 배치를 임의 변경하지 않았다.
- 증분 21 검증: 전용 계약 `22/22`, 전체 Vitest `45파일·348/348`, diff-check 통과.
  다음 gate는 Artist 3이 zero-pixel preflight에 계약을 수신한 뒤 사용자 승인을 받는 것이다.
  승인 전 픽셀·finalizer·manifest는 만들지 않는다.
- **증분 22 — D1 첫 주문 가이드 비침범 토글(2026-08-01):**
  사용자 플레이에서 우상단 전체 단계 목록이 InGame 장면을 가리는 문제를 수정했다. `d1-guide`는
  기본 접힘에서 제목·현재 단계·다음 행동만 유지하며 `전체 보기`·`접기` 버튼으로 단계 목록을
  토글한다. 토글은 `aria-expanded`·`aria-controls`를 제공하고 가이드 진행·대상 강조·영업 상태를
  변경하지 않는다. 정리 필요 좌석은 하단 조작 카드에서 비활성 빈 자리로 보이지 않도록
  `정리 필요`·`3초 동안 눌러 정리` 문구와 파란 상태 강조를 제공하고 기존 3초 홀드 입력을 유지한다.
  펼친 목록은 기존 `pointer-events:none`을 해제해 휠·트랙패드 스크롤이 실제 동작한다. 첫 주문 완료
  뒤에는 미사용 상태였던 전체 영업 안내를 연결해 23:30 자동 마감까지 남은 실제 시간과 마감 후
  남은 주문·정리·그릴 위험 공정 수를 표시한다. 첫 주문 임시 저장 키는 공개 새 게임·처음부터 다시
  보기·개발 `reset=1`에서 제거해 이전 가이드 상태가 새 캠페인으로 누출되지 않으며, 같은 영업의
  새로고침 복구는 유지한다. 전용 Playwright는 Chromium FHD/720 `4/4`,
  관련 영업 도메인·UI 포트는 `12/12`를 통과했다.
- 기획 결정 대기: `basePrice=3 × 1.1`은 현 `Math.round` 계약에서 다시 `3`이므로
  `ingredient-chicken-t2`의 실제 판매가 상승이 0이다. 이번 증분은 가격·배수·반올림을 임의 변경하지
  않았으며 PM이 최소 가격 단위·누적 방식·효과 수치 중 하나를 별도로 확정해야 한다.
- 위 결과는 최신 프로덕션 기획의 다중 손님·독립 화면·드링크·자동 제공을 검증하지 않는다.
- 남은 위험(후속 증분): 다중 동시 주문·6석 손님 운영·대기 게이지·정리(작업 006/`GPL-003`), 드링크 실제
  공정·품질·공용 준비 목록 교차 이동·자동 제공(`GPL-004`), 승인 아트 적용, 실제 GPU 측정 및 사용자 관찰.
