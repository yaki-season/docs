# 개발자 1 - 009 D1 전체 영업일·정산과 D2 전환

- 문서 버전: `v1.11.0`
- 최종 변경일: `2026-07-31`
- 상태: `진행 중`
- 담당자: `개발자 1`

## 참조 spec

- `spec/gameplay/GPL-002_30일_캠페인과_영업일_루프.md` - `GPL-002` - `v1.10.0`
- `spec/gameplay/GPL-003_주문_손님_좌석과_러시_운영.md` - `GPL-003` - `v4.12.0`
- `spec/gameplay/GPL-004_조리_스테이션과_품질_서빙.md` - `GPL-004` - `v1.39.0`
- `spec/gameplay/GPL-005_경제_성장_직원과_보상.md` - `GPL-005` - `v2.22.0`
- `spec/system/SYS-003_전체_게임_런타임_저장_성능.md` - `SYS-003` - `v2.0.0`
- `spec/ui/UI-002_전체_게임_화면_입력_접근성.md` - `UI-002` - `v5.25.0`

## 목표

현재 첫 손님 수직 슬라이스를 D1 전체 영업일로 확장해 추가 손님·좌석 정리·마감·정산·자동
저장과 D2 전환까지 끊김 없이 플레이할 수 있게 한다.

## 사용자 확정 실행 조건

- D1은 첫 손님 반응에서 끝나지 않고 전체 영업일을 완료한다.
- 아사노 아키와 츠키오카 세이지만 고정 인물이며 나머지 D1 손님은 이름 없는 엑스트라다.
- 조립·그릴·드링크는 클릭 기반이고 플레이어 신체 부위를 표시하지 않는다.
- 최초 공개 시 D1 사용자 노출 placeholder는 0개여야 한다.

## 범위

### 포함

- 츠키오카 완전 가이드 뒤 시간대별 엑스트라 손님 입장·주문·부분/최종 제공
- 6석·그룹·대기·자동/부분 제공과 공용 준비 목록의 프로덕션 연결
- 퇴장 좌석 3초 정리, 정리 전 재배정 금지
- 마감 시 신규 입장 중단, 남은 주문·좌석 정리와 숯불 낮추기
- 정산 5단계, 매출·팁·명성·품질·대기·이탈 집계와 성장 결과
- `day-complete` 저장 후 D2 개방과 전환
- 본편 시작부터 고정 6칸, D1 첫 네기마 3개의 1~3번 칸 배치·동시 타이머 시작
- D1 양면 조리물의 방향·접촉면·면별 누적 시간 보존과 재투입 불변식

### 제외

- S0 프롤로그 화면과 이야기 UI — 개발자 2 작업 004
- D2·D3 영업 규칙과 D4 예고 — 개발자 1 작업 010
- 조립·그릴 아트 제작과 승인 — Artist 1 작업 000
- 드링크·서빙·정리·엑스트라·정산 아트 제작과 승인 — Artist 3 작업 025
- S0 아트 제작과 승인 — Artist 2 작업 023
- 배포·전체 공개 검증 — 개발자 2 작업 006

## 작업 절차

1. 작업 005·006의 프로덕션 화면·손님 모델과 작업 000의 D1 주문·조리 상태를 연결한다.
2. D1 시간대별 손님 생성과 활성 주문·위험 공정 상한을 콘텐츠에서 읽는다.
3. 첫 손님 뒤 엑스트라 입장부터 퇴장·정리까지 전체 상태 흐름을 구현한다.
4. 마감·정산·성장 결과를 기존 영업일 집계와 연결한다.
5. 작업 008의 체크포인트에 정산 결과를 한 번만 반영하고 D2를 개방한다.
6. 기존 2칸·2+1 대기열과 가변 `grillSlots`를 제거하고, 6칸 중 첫 3칸 배치 완료 시
   세 네기마의 앞면 독립 타이머를 함께 시작한다.
7. 스테이션 전환·재투입에서도 양면 조리물의 `orientationFaceDown`·`contactFace`와 면별
   누적 시간을 보존한다.
8. 모의 시간 종단과 두 기준 해상도 E2E를 추가한다.
9. 모든 D1 사용자 노출 상태의 `screenId/stateId/componentId/requiredAssetId`와 placeholder 여부를
   단일 inventory로 내보내고 Artist 1·3과 개발자 2 작업 003이 같은 표를 소비하게 한다.

## 의존성과 인계 조건

- 선행 작업: 개발자 1 작업 005·006·008, 개발자 2 작업 000·003·004·005, 개발자 3 작업 005
- 후속 작업: 개발자 1 작업 010, 개발자 2 작업 006
- Artist 1 작업 000과 Artist 3 작업 025의 승인 상태별 manifest ID만 소비하며 review 경로를
  직접 참조하지 않는다.
- stable ID가 아직 정해지지 않은 신규 상태는 명시적 placeholder ID로 먼저 동결하고,
  Artist에게 파일명·component 의미를 추측하게 하지 않는다.
- 상태·component inventory를 변경하면 해당 owner와 개발자 2 작업 003에 같은 변경으로 알리고,
  이미 제작 중인 stable ID의 의미를 재사용하지 않는다.

## 완료 기준

- [x] S0 종료 뒤 D1 전체 영업이 최대 8분 목표 안에서 끝난다.
- [x] 첫 손님 이후 엑스트라 손님·주문·부분 제공·정리 흐름이 반복된다.
- [x] 좌석 정리 전 재배정이 발생하지 않고 3초 완료 이벤트가 한 번만 적용된다.
- [x] 마감·정산 5단계와 매출·팁·명성·품질·대기·이탈 집계가 일치한다.
- [x] 중단·재개와 중복 입력에도 정산 보상이 중복 지급되지 않는다.
- [x] D1 완료 저장 뒤 D2가 개방되고 새로고침 후 같은 상태로 이어진다.
- [x] D1 그릴은 처음부터 6칸이며 네기마 3개를 1~3번 칸에 모두 배치하기 전에는 굽기가 시작되지 않고, 세 번째 배치 뒤 세 앞면 타이머가 같은 시점에 독립적으로 시작한다.
- [x] 각 제작물은 접촉면 한 면만 진행하고 뒤집기 공중·그릴 밖에서는 양면이 정지하며, 재투입·저장 복구 뒤 방향과 양면 누적 시간을 보존한다.
- [ ] 최초 공개 D1 사용자 노출 placeholder가 0개다.

## 구현 및 검증 결과

- app 구현 위치:
  - `src/domain/businessDay/d1BusinessDay.js` — 결정적 D1 파동·좌석·주문·부분 제공·그룹·정리·마감·정산 도메인
  - `src/application/businessDay/d1BusinessDayRuntime.js` — 작업 008 캠페인 런타임의
    `day-start`·`day-complete` 저장과 D1 정산 단일 commit 연결
  - `src/application/businessDay/d1BusinessDayUiPort.js` — DOM 없이 6석·주문·부분 제공·정리·
    마감·정산 view model과 stable intent/error code를 제공하는 개발자 2 연결 포트
  - `src/application/businessDay/d1BusinessDayBrowserSession.js` — S0 저장을 불러오거나 개발
    캠페인을 시작하고 D1 `day-start` 체크포인트·`D1BusinessDayRuntime`·
    `D1BusinessDayUiPort`를 조립하며, 완료 저장이면 D2 pre-open을 그대로 재개하는 브라우저 adapter
  - `src/application/ports/d1BusinessDayDefinition.js` — Developer 3의
    `d1-release-definition/schemaVersion=1` 하나만 받아 domain definition으로 투영하는 consumer port.
    seat/wave/customer/group/timing/price/prerequisite/totals 누락·버전·로드 실패는
    `D1_DEFINITION_*` 오류로 반환하고 개발 fixture fallback을 금지
  - `src/render/cookStations.js` — 렌더러 독립 면별 시간·접촉면·공중 뒤집기·재투입·snapshot과
    고정 6칸/첫 3개 동시 시작 `createD1CookStations`. `slotViews().nextAction`은
    `clickSlot()`과 같은 판정으로 empty/staged/wait/flip/retrieve를 view-only 투영
  - `src/config/d1GrillLayout.js`, `src/config/screenLayout.js`, `src/config/d1Layout.js` —
    두 프로덕션 화면이 공유하는 `grillSlot0~5` visual rect·raycast hit target과 대기/완료 tray 계약.
    승인 `MDL-NEGIMA-GRILL-RAW` station consumption R3의 slot0 FHD visual
    `545,257,131,516`·anchor `609.6,515`를 153.6px lane 간격으로 6칸에 전개하고
    raw/cooking/flipped/proper/burnt가 같은 위치·스케일 transform을 소비한다.
    승인 `ST-GRILL-FINISHED-TRAY@R6`의 완료 tray FHD bounds `1534,123,266,354`·
    anchor `1643,301`, UI reserved bounds, background/grate/tray/food overlay 좌표 책임을
    정규화 단일 원본으로 제공
  - `src/render/sceneMath.js` — off-axis billboard의 대각선 거리 팽창을 카메라 전방축 깊이로
    정정해 visual/hit mesh가 정규화 rect 바깥까지 커지지 않게 함
  - `src/render/productionRenderer.js`, `src/render/d1SceneRenderer.js` — visual mesh와 투명 hitRect
    raycast mesh를 분리해 클릭 영역 변경이 승인 시각 위치를 이동·변형하지 않게 함
  - `src/config/screenLayout.js`, `src/render/productionRenderer.js` — 좌석 손님을 카운터 뒤 상체
    crop으로 배치하고, 손님 actor를 직접 클릭하는 별도 완전 투명 주문·서빙·정리 hit mesh를 제공
  - `src/render/grillMaterial.js`, `src/render/cookStations.js`, `src/d1-game.js` — 기존
    `contactFace=null` 0.3초 뒤집기와 양면 각 8초 domain 계약을 유지하면서 실제 slot mesh의
    `0→π` 회전, double-sided 뒷면, 배치·뒤집기·회수 조작 힌트를 연결
  - `src/render/cookStations.js` — active slot의 최소 뒤집기 시간을 제거하고 under 면을
    번갈아 보충 조리하며, 양면 준비 뒤 최종 누적 시간으로 회수 품질을 재분류
  - `src/domain/businessDay/d1BusinessDay.js` — `order.guided`의 `nextGuidedMenu` 제공 강제를
    제거해 선택한 손님의 일치하는 미제공 line을 주문·손님 순서와 무관하게 적용
  - `src/game.js` — 구형 1칸과 `grillSlots` 구매 소비를 제거하고 고정 6칸 factory를 연결
    하며, production이 실제 fetch한 D1 day·upgrade 원본과 runtime 적용값을
    `__prodDebug.contentContract()` 하나로 공개해 E2E의 데이터 상수 복제를 차단
  - `src/d1-game.html`, `src/d1-game.css`, `src/d1-game.js` — 단일 손님 controller를 제거하고
    프로덕션 6석 renderer·`customerAdapter`·`D1BusinessDayUiPort`를 연결. 츠키오카와 엑스트라
    3명/주문 4건/항목 9개, 부분·최종 제공, 3초 정리, 7분 마감 drain, 숯불, 정산 5단계,
    단일 보상 저장과 D2 전환을 실제 좌석 raycast 조작으로 제공
  - `src/assets/d1RuntimeInventory.js` — 42개
    `screenId/stateId/componentId/requiredAssetId/semanticOwner/bindingState` 단일 inventory와
    owner별 placeholder 보고
  - `src/campaign-runtime.js` — 개발자 2가 소비할 D1 공개 interface export
  - `tests/fixtures/business-days/d1-full-day.json` — 개발자 3 정식 콘텐츠 전의 결정적 통합 fixture
  - `tests/unit/d1BusinessDay.test.js`, `tests/unit/cookStations.test.js`, `tests/unit/d1GrillLayout.test.js`,
    `tests/unit/d1RuntimeInventory.test.js`, `tests/integration/d1CampaignIntegration.test.js`,
    `tests/integration/d1BusinessDayUiPort.test.js`,
    `tests/integration/d1BusinessDayBrowserSession.test.js`,
    `tests/e2e/d1-campaign-runtime.spec.js`, `tests/e2e/d1-business-day.spec.js`,
    `tests/e2e/d1-game.spec.js`, `tests/e2e/production-growth.spec.js`,
    `tests/e2e/production-screens.spec.js`, `tests/e2e/production-grill-gauge.spec.js`,
    `tests/e2e/d1-grill-layout-overlay.spec.js`
- 구현 기준 spec 버전: 위 참조 spec
- 구현 기준 태스크 버전: `v1.11.0`
- 검증 방법:
  - `npx vitest run tests/integration/d1BusinessDayUiPort.test.js tests/integration/d1CampaignIntegration.test.js tests/unit/d1BusinessDay.test.js`
  - `npx playwright test tests/e2e/d1-campaign-runtime.spec.js`
  - `npx playwright test tests/e2e/d1-game.spec.js tests/e2e/d1-business-day.spec.js`
  - `npx playwright test tests/e2e/d1-game.spec.js tests/e2e/production-growth.spec.js tests/e2e/production-screens.spec.js --project=chromium-1280x720 --project=chromium-1920x1080`
  - `npm test`
  - `npm run verify`
  - `npx playwright test tests/e2e/grill-tuner.spec.js --grep '익힘 단계 미리보기'`
- 검증 결과:
  - 009 관련 단위·통합 `27/27` 통과. 새 UI port 경계 테스트 `2/2`는 기존
    `SEAT_IDS`·`buildSeatStates`와 좌석 ID·주문 label·intent error·마감/정산 view를 함께 검증
  - 공개 UI port를 통한 브라우저 localStorage D1 전체 영업·정산·D2 저장 복구 `2/2` 통과
  - 전체 Vitest `28`파일 `228/228` 통과
  - 전체 verify에서 자산 `8개`, 참조 이미지 `36장`, Vitest `224개`, Playwright
    `116개` 통과·`2개` 스킵. 기존 셰이더 튜너 미리보기 `2개`는 두 해상도에서 실패했고,
    드링크 홀드·성능 각 `1개`는 재시도 통과
  - 셰이더 튜너 실패는 격리 명령에서도 `2개` 재현. 009 런타임을 사용하지 않는
    `src/main.js`/`src/render/grillTuner.js` 경로이며 이번 소유 범위의 D1 화면 파일은 수정하지 않음
  - 프로덕션 고정 6칸 연결 뒤 관련 단위 `16/16`, 전체 Vitest `30파일·237/237`, 실제 D1/프로덕션
    FHD·720 E2E `28/28`, 고정 6칸 게이지 FHD·720 `2/2` 통과
  - 첫 전체 `verify`는 자산 `8개`·참조 이미지 `36장`·Vitest `237개`·Playwright `129개` 통과,
    `2개` 스킵. 고정 6칸 전제 누락 게이지 2개는 갱신 후 `2/2` 통과했고, `SCN-001` 튜너 2개는
    격리 재현, 드링크 홀드·성능은 격리 재실행 통과
  - 실제 화면 전체 영업 증분 관련 단위·통합 `26/26`, 전체 Vitest `32파일·243/243` 통과
  - `d1-game`의 고정 6칸 첫 주문 회귀와 전체 영업·영업 중 복구 E2E는
    FHD/720 합계 `10/10` 통과. 전체 영업 종단은 420,000ms(7분), 방문 4명·완료 주문 4건·
    제공 9개·정리 4석·매출 36·팁 8·합계 44·명성 12·정산 원장 1건·D2 pre-open을 확인
  - 최신 전체 `npm run verify`: 자산 `8개`, 참조 이미지 `36장`, Vitest `243/243`,
    Playwright `151개` 통과·`2개` 스킵. 이번 `d1-business-day`·`d1-game`은 두 해상도에서
    모두 통과. 독립 `SCN-001` 튜너 미리보기 `2개`는 FHD/720에서 재시도 후 실패했고,
    기존 프로덕션 드링크 홀드 FHD `1개`는 첫 실행 실패 뒤 재시도 통과
  - 승인 master R3 좌표 정합 뒤 레이아웃·조리 단위 `18/18`, 전체 Vitest 기준선
    `33파일·253/253`, 실제 조리→양면 각 8초 적정→finished tray 전달·tray 클릭 FHD/720
    `2/2`, R3 master overlay FHD/720 `2/2` 통과. 슬롯 visual/hit은 연속 석쇠 safe bounds 안에
    있고 tray visual/hit mesh UUID가 분리됨을 확인
  - 저장 envelope/campaign payload에는 layout 좌표·slot rect·tray anchor가 없어 저장 schema와
    migration을 추가하지 않았다. Developer 3 콘텐츠 완료 뒤 최종 `verify`는 자산 8개·reference
    36장·Vitest `259/259`, Playwright `156개` 통과·`2개` 스킵·`8개` 실패. 이번 D1 조리·tray·
    overlay는 FHD/720 모두 통과했고, 실패는 기존 `SCN-001` 튜너 2건과 신규 D1 입장 30초·가격 3을
    구값 12초·100·정산 250으로 기대하는 groups/growth/settlement 회귀 6건으로 범위 밖
  - 결정적 회귀 정정 뒤 관련 단위 `32/32`, 지정 Playwright 4파일은 1280×720 `13/13`,
    1920×1080 `13/13`, 합계 `26/26` 통과. rush는 production이 적용한 `spawnIntervalSec`와 로드 day 원본이 같음을
    검증하고, growth·settlement는 같은 원본의 `basePrice=3`·품질 배수와 로드 upgrade 비용·효과에서
    기대값을 계산한다. `SCN-001` 튜너도 `previewStage(0)` 즉시·다음 프레임 유지로 두 viewport 통과
  - 최신 `npm test`는 35파일 264개 중 이번 변경과 무관한 fully-qualified semanticOwner 병행 전환
    회귀 3건(`d1RuntimeInventory` 2, `runtimeAssetResolver` 1)만 실패해 `261/264`. Artist finalizer·
    manifest·`runtimeAssetResolver`와 타 역할 inventory는 수정하지 않았다
  - 정식 D1 전환 범위 감사에서 fixture와 정식 데이터의 목표 420,000ms·상한 2/1·주문 ID 4개·
    항목 9개는 일치했다. 그러나 정식 계약에는 6개 `seatIds`, segment별 결정적 `atMs`,
    `leave/cleanup` 시간, 메뉴별 가격(현재 fixture beer 6/negima 3), 엑스트라 runtime ID와
    office pair group ID 생성 규칙이 없다. 이를 임의 추론하면 7분 파동·매출 36/총 44·저장/E2E
    기대값이 바뀌므로 이번 증분에서는 fixture 교체를 실행하지 않았다
  - Developer 3 작업 008 `v1.0.2`의 release builder·schema handoff를 consumer에 적용했다.
    consumer characterization `16/16`, 실제 builder output 통합 `1/1`, release/브라우저 세션 포함
    타깃 `22/22`, 전체 `npm test` `41파일·294/294`가 통과했다
  - `d1-game.js`와 browser session에서 `D1_DEVELOPMENT_DEFINITION_URL` 및
    `/tests/fixtures/business-days/d1-full-day.json` 직접 fetch를 제거했다. production은
    `/content/releases/d1-business-day-definition.v1.json`의 versioned release 하나만 읽으며,
    없거나 누락되면 `D1_DEFINITION_LOAD_FAILED`/`CONTRACT_INVALID`를 노출하고 fallback하지 않는다
  - E2E에서 Developer 3 builder의 실제 output을 위 단일 URL 응답으로 공급한 전체 영업 golden은
    1280×720 `5/5`, 1920×1080 `5/5`, 합계 `10/10` 통과. S0→D1, 4명·4주문·9항목,
    생맥주 1→네기마 3, 부분 제공, 3초 정리, 마감 drain, 5단계 정산, 단일 저장, 새로고침,
    D2 pre-open을 확인했다
  - 승인 R6 PNG SHA
    `51abf390cbf31d40b50a7d99f08a5d37a2da70df6cf94d405788538bad7d9184`와 실제 alphaBBox
    `1534,123,266,354`를 확인했다. 새 visual/hit/reserved bounds는 FHD에서 같은 값,
    720p에서 `1022.6666667,82,177.3333333,236`; anchor는 기존 FHD `1643,301`,
    720p `1095.3333333,200.6666667`을 유지한다
  - actual-alpha overlay와 production renderer 투영 bounds, tray 중심·네 외곽 hit·bounds 밖
    4px miss, slot0~5 hit mesh를 FHD/720 2회 반복해 `8/8` 통과했다. 첫 3개 동시 시작과
    앞면 8초→뒤집기→뒷면 8초→finished 전달→tray 클릭도 양 viewport에서 통과했다
  - Developer 3 작업 009 `v1.0.1` artifact
    `/content/releases/d1-business-day-definition.v1.json`
    (SHA `b1535f2f6b5a7f3fde6f32eb4cb016007df5ad790d760f92456ab70098507941`)을 수신했다.
    builder drift check·HTTP `200 application/json`을 확인하고, 성공 경로 `page.route` 없이
    전체 영업·영업 중 복구 FHD/720 `4/4`와 static boot FHD/720 `2/2`를 통과했다
  - 무주입 golden은 방문 4·주문 4·항목 9·정리 4, 매출 36·팁 8·합계 44·명성 12,
    settlement ledger 1, 저장·새로고침 뒤 `d2/pre-open`, 중복 보상 없음이다.
    404·지원하지 않는 schema version은 `D1_DEFINITION_LOAD_FAILED`/
    `D1_DEFINITION_VERSION_UNSUPPORTED`로 실패하고 fixture fallback하지 않는다
  - 최신 `npm test`는 `42파일·299/299`, R6 레이아웃 단위 `6/6`, 변경 파일
    `git diff --check`가 통과했다. R6 overlay/raycast·첫 주문 조리·무주입 전체 영업/복구·
    static boot/error 최종 Playwright 묶음은 FHD/720 합계 `20/20` 통과했다. 공유 QA 터미널의
    병행 `npm run verify`가 진행 중이므로 충돌을 피하기 위해 이번 터미널에서 전체 verify는
    중복 실행하지 않았다
  - 사용자 조작 피드백 정정 뒤 관련 단위 `22/22`, `d1-game`의 실제 손님 actor 중심 주문 클릭·
    투명 target·그릴 slot mesh π 회전 FHD/720 `6/6`, 전체 영업·복구·R6 tray/slot·
    프로덕션 조리/서빙 회귀 FHD/720 `22/22`, 전체 Vitest `42파일·300/300`이 통과했다.
    actor visual과 interaction mesh는 계속 별도 객체이며 좌석·조리 domain과 저장 payload/schema는
    변경하지 않았다
  - `GPL-004 v1.39.0`·`GPL-003 v4.12.0` 정정 뒤 Developer 1 타깃 단위·통합 `28/28`,
    Developer 3 작업 011 독립 계약 `4/4`, 전체 Vitest `43파일·308/308`, Chromium FHD/720
    전체 영업·영업 중 복구·프로덕션 조리/서빙 `18/18` 통과. 앞면 `3+5초`·뒷면 `4+4초`
    누적 `Perfect`, 조기 반복 뒤집기·회전/입력 잠금, snapshot 복구, 네기마 선제공,
    늦게 주문한 선택 손님 선제공, 잘못된 손님·메뉴 차단을 확인했다
  - 승인 raw R3 footprint 정합 뒤 layout 단위 `9/9`, 조리·선택 손님 제공 단위/통합 `29/29`,
    전체 Vitest `43파일·311/311` 통과. 실제 master overlay와 production visual/raycast mesh
    FHD/720 `4/4`, 조기 뒤집기·0.3초 잠금·선택 손님 제공·조리→양면 8초→finished tray
    FHD/720 `12/12`를 통과했다. 기존 hitRect·R6 tray·저장 payload/schema는 변하지 않았다
  - `nextAction` projection 뒤 `cookStations` 단위 `16/16`, Developer 3 작업 011 계약 포함
    `21/21`, 전체 Vitest `43파일·316/316` 통과. 앞 3초→0.3초 회전 종료→뒷면 0초는
    `nextAction=flip`, 양면 8초는 `nextAction=retrieve`이며 같은 `clickSlot()` 결과와 일치한다.
    snapshot `stateVersion=1`, 양면 시간·접촉면·캠페인 저장 schema는 변경하지 않았다
- 남은 위험:
  - `basePrice=3 × 1.1`이 현 `Math.round`에서 3으로 남는 성장 효과 0 문제는 별도 PM 결정 항목이다.
  - Artist 1·3 finalizer handoff 전 엑스트라 `CH-EXTRA-COMMUTER-SERVICE`·
    `CH-EXTRA-SOLO-SERVICE`, 숯불 `ST-CHARCOAL-CORE`, 정산 `UI-ECONOMY-ICONS` 등
    inventory pending 항목은 화면에 stable ID가 붙은 명시적 개발 placeholder로 남는다.
  - 실제 정적 release 입력과 전체 영업 기능 완료 기준은 충족했다. 최초 공개 placeholder 0
    조건은 Artist 1·3 finalizer와 Developer 2 binding에 남아 있으므로 태스크 상태는 `진행 중`을 유지한다.

## 변경 이력

| 이전 버전 | 새 버전 | 날짜 | 변경 유형 | 근거 spec 버전 | 변경 내용 | 재작업 영향 |
|---|---|---|---|---|---|---|
| 없음 | `v1.0.0` | `2026-07-30` | 최초 생성 | `GPL-002 v1.10.0`, `GPL-003 v4.11.0`, `GPL-004 v1.37.0`, `GPL-005 v2.22.0`, `SYS-003 v2.0.0`, `UI-002 v5.25.0` | D1 전체 영업·정산·저장·D2 전환 작업 생성 | 없음 |
| `v1.0.0` | `v1.1.0` | `2026-07-30` | 범위 동기화·상태 갱신 | `GPL-004 v1.37.0 → v1.38.0`, 나머지 참조 spec 동일 | 본편 고정 6칸, D1 첫 네기마 3개의 첫 3칸 배치·동시 시작과 양면 조리물의 방향·접촉면·면별 시간·재투입 보존 검증을 추가하고 사용자 확인에 따라 `진행 중`으로 전환 | 기존 2칸·2+1 대기열·가변 `grillSlots`를 교체하며 D5 카와는 009 범위에서 제외 |
| `v1.1.0` | `v1.2.0` | `2026-07-30` | 다중 Artist 구현 인계 추가 | 참조 spec 동일, Artist 1 작업 000 `v1.1.0`, Artist 3 작업 025 `v1.0.0` | D1 조립·그릴과 비조리 공개 필수 pack의 producer를 분리하고 `screenId/stateId/componentId/requiredAssetId` placeholder inventory 제공 의무를 추가 | gameplay 상태 의미는 유지하며 Artist 1·3과 개발자 2 작업 003이 동일 ID 계약으로 병행 가능 |
| `v1.2.0` | `v1.2.1` | `2026-07-30` | 구현·검증 중간 결과 | 참조 spec 동일 | D1 7분 모의 영업·4명/4주문/9항목·부분 제공·3초 정리·5단계 정산·단일 보상 commit·D2 저장 복구, 고정 6칸 첫 3개 동시 시작과 면 상태 보존, 42개 runtime inventory를 공개 interface와 테스트로 구현 | 도메인·저장 계약은 통과했으나 병행 프로덕션 화면·정식 콘텐츠·아트 handoff 연결 전까지 `진행 중` 유지. 전체 verify의 기존 셰이더 튜너 실패 2개를 분리 기록 |
| `v1.2.1` | `v1.2.2` | `2026-07-30` | 프로덕션 연결 포트 구현 | 참조 spec 동일 | 기존 6석 렌더 계약과 D1 도메인 사이에 DOM 없는 `D1BusinessDayUiPort`를 추가하고 한국어 메뉴·부분 제공·중복 intent·stable error·마감/정산 화면·D2 저장 복구를 공개 export와 브라우저 테스트로 검증 | 기존 D1 화면 파일을 수정하지 않고 개발자 2가 view/intent를 직접 연결할 수 있게 됨. 실제 화면 binding과 정식 콘텐츠·아트 handoff 전까지 `진행 중` 유지 |
| `v1.2.2` | `v1.3.0` | `2026-07-30` | 프로덕션 고정 6칸 연결 | 참조 spec 동일 | `game.js`의 1칸·업그레이드 확장과 `d1-game.js`의 2칸을 고정 6칸/첫 3개 동시 시작 factory로 교체. slot0~5 rect/hit target·완료 tray 계약과 FHD/720 실제 클릭 검증 추가 | 단일 손님 화면은 전체 영업 runtime 연결 전이므로 `진행 중` 유지. SCN-001·Artist stable ID·manifest는 무변경 |
| `v1.3.0` | `v1.4.0` | `2026-07-30` | 프로덕션 전체 영업 연결 | 참조 spec 동일 | `d1-game.js`의 단일 손님 controller를 프로덕션 6석 renderer와 `D1BusinessDayRuntime`·`D1BusinessDayUiPort` 소비 구조로 교체하고 S0→D1, 4주문·9항목, 부분/최종 제공, 3초 정리, 7분 마감 drain, 숯불, 정산 5단계, 단일 보상 commit, D2 저장/새로고침을 실제 사용자 조작으로 연결 | 기능 완료 기준은 충족. 정식 콘텐츠와 Artist 1·3 placeholder 0 finalizer 전까지 `진행 중` 유지하며 Developer 2 shell·Artist namespace·manifest는 무변경 |
| `v1.4.0` | `v1.5.0` | `2026-07-30` | 승인 master R3 runtime 좌표 정합 | 참조 spec 동일, 사용자 승인 `CM-GRILL-STATION-QUEUED-SELECTION R3` | 완료 tray를 FHD 1534·130·218·342와 anchor 1643·301로 정정하고 visualRect/hitRect/reservedBounds·레이어 책임을 분리. 여섯 slot 석쇠 내부 overlay, tray 클릭, 조리 완료 전달을 FHD/720에서 검증 | 조리 상태 머신·stable ID·manifest·저장 schema는 무변경. Artist base parity와 Developer 2 receipt rail은 이 reserved bounds를 소비 |
| `v1.5.0` | `v1.6.0` | `2026-07-30` | 결정적 회귀 정정·정식 콘텐츠 전환 감사 | 참조 spec 동일, Developer 3 작업 005 `v1.1.0` | production day/upgrades 원본과 runtime 적용값을 단일 debug 계약으로 공개하고 rush·growth·settlement의 구형 수치 상수를 제거. 튜너 0 override 포함 지정 E2E FHD/720 26/26 통과. 정식 D1과 fixture의 일치·누락 필드 및 회귀 범위를 대조 | 가격·반올림과 정식→runtime 누락 변환 정책은 PM/Developer 3 결정 전 보류. 작업 010·Artist asset 경로는 착수/변경하지 않음 |
| `v1.6.0` | `v1.7.0` | `2026-07-30` | release definition consumer·handoff 적용 | 참조 spec 동일, Developer 3 작업 008 `v1.0.2` | versioned release 하나만 받는 consumer port와 명시적 누락/로드 오류를 구현하고 production의 개발 fixture 직접 fetch를 제거. 실제 builder output을 단일 URL로 공급한 FHD/720 전체 영업 golden 10/10, 전체 Vitest 294/294 통과 | materialized release JSON handoff 전 일반 정적 진입은 명시적 404. content를 대신 생성하거나 fallback하지 않으며 작업 010은 무변경 |
| `v1.7.0` | `v1.8.0` | `2026-07-30` | 승인 tray R6 좌표·정적 release 무주입 golden | 참조 spec 동일, 사용자 승인 `ST-GRILL-FINISHED-TRAY R6`, Developer 3 작업 009 `v1.0.1` | tray visual/hit/reserved를 FHD 1534·123·266·354로 정합화하고 off-axis mesh 팽창을 수정했다. 실제 R6 alpha·raycast와 materialized release HTTP 200·무주입 FHD/720 전체 영업/복구를 검증했다 | anchor·slot0~5·조리 상태·저장 schema는 유지. Developer 2는 새 reserved bounds를 소비하며 Artist/manifest/runtime 등록은 별도 gate |
| `v1.8.0` | `v1.8.1` | `2026-07-30` | 사용자 조작 피드백 정정 | 참조 spec 동일 | 손님을 카운터 뒤 상체로 배치하고 보이는 테이블형 주문 target을 손님 actor 위 투명 hit으로 교체했다. 조리 상태 머신은 유지하면서 0.3초 `0→π` 실제 mesh 회전과 8초 뒤집기·회수 힌트를 연결했다 | 영업 domain·저장 schema·R6 tray·slot0~5 좌표·Artist/manifest는 무변경. FHD/720 직접 클릭과 뒤집기 시각 회귀를 추가 |
| `v1.8.1` | `v1.9.0` | `2026-07-30` | 자유 뒤집기·손님 중심 제공 domain 정정 | `GPL-004 v1.38.0 → v1.39.0`, `GPL-003 v4.11.0 → v4.12.0` | under 반복 뒤집기와 회수 시점 최종 누적 품질 계산을 구현하고, guided·손님 접수 순서가 선택 손님의 일치 메뉴 제공을 막지 않게 했다 | UI·CSS·아트·manifest·저장 schema는 무변경. 새 view 소비 계약을 Developer 2 작업 010에 별도 dispatch |
| `v1.9.0` | `v1.10.0` | `2026-07-30` | 승인 raw R3 footprint runtime 정합 | 참조 spec 동일, 사용자 승인 `MDL-NEGIMA-GRILL-RAW` station consumption R3 | 승인 slot0 visual/anchor를 고정 6칸 screen-space 계약으로 전개하고 visualRect·기존 hitRect·상태 공통 transform을 공개했다 | 뒤집기·제공 domain은 재작업하지 않았고 R6 tray·저장 schema·아트·manifest는 무변경. Artist 1·Developer 2 handoff 추가 |
| `v1.10.0` | `v1.11.0` | `2026-07-31` | 그릴 다음 행동 도메인 projection | `GPL-004 v1.39.0`, Developer 3 작업 011 `v1.0.2` | `slotViews().nextAction`을 `clickSlot()`과 같은 판정으로 제공하고 앞 3/뒤 0은 flip, 양면 8/8은 retrieve로 고정했다 | Developer 2 작업 011 exact handoff와 Artist 3 좌석 geometry handoff를 분리. UI·아트·manifest·D2/D3 기능·저장 schema는 무변경 |
