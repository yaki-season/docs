# 통합 QA·릴리스 - 001 통합 기준선·회귀와 공개 게이트

- 상태: `진행 중`
- 담당자: `통합 QA·릴리스`

## 참조 spec

- `spec/system/SYS-003_전체_게임_런타임_저장_성능.md` - `SYS-003` - `v2.0.0`
- `spec/system/SYS-004_app_프로젝트와_소스코드_아키텍처.md` - `SYS-004` - `v3.1.0`
- `spec/ui/UI-003_전체_게임_상세_화면_설계.md` - `UI-003` - `v3.0.0`
- `spec/art/ART-003_런타임_아트_에셋_목록과_제작_계약.md` - `ART-003` - `v7.0.0`
- `spec/qa/QA-002_전체_게임_기능_성능_출시_검증.md` - `QA-002` - `v1.3.0`

## 목표

동시에 진행 중인 개발자·Artist 산출물을 기능이나 승인 아트의 의미를 바꾸지 않고 하나의 검증 가능한
기준선으로 통합하고, S0·D1 최초 공개 gate의 회귀·에셋·handoff 상태를 자동 보고한다. 추가로
S0·D1 범위의 재생성 가능한 ignored 임시 산출물과 obsolete 후보를 안전하게 분류·감사하여, 승인
evidence·runtime provenance·진행 중 변경을 보존한 저장소 위생 기준선을 관리한다. 자동·제작자
검증과 별도로 PM이 요청하는 스테이지별 사람 화면 테스트의 재현 환경·결과·증거를 관리하되,
사용자 승인이나 제품 의미를 대신 판단하지 않는다.

## 사용자 확정 실행 조건

- 최신 Chrome·Edge, 마우스·키보드, `1920×1080`과 `1280×720`을 검증한다.
- S0·D1 최초 공개의 사용자 노출 placeholder는 0개여야 한다.
- 자동 테스트와 제작자 수동 검수에 더해 S0·D1 중간 slice와 전체 구동의 사람 화면 `PASS`를
  공개 gate로 사용한다.
- PM의 중간 사람 검수 1회는 `QA-002`의 최종 비개발자 관찰 플레이테스트 표본을 대체하지 않는다.
- 원격 플레이 기록을 수집하지 않고 진단 정보에도 저장 payload·개인정보를 포함하지 않는다.

## 범위

### 포함

- `app`·`docs`·`art-workspace`의 역할별 dirty 변경 및 계약 드리프트 read-only 감사
- 역할별 인계 순서, 통합 기준 commit과 재검증 명령 기록
- Vitest·Playwright·assets·reference image·pipeline 검증 결과 집계
- 반복 실패, flaky, 성능 예산, viewport·Chrome·Edge 차이 분류와 소유자 배정
- manifest binding·placeholder·404·console 오류와 `semanticOwner` 중복 공개 gate
- 승인·소비 화면·최적화·finalizer·dry-run receipt·명시적 write 단계 감사
- ignored test output·일시 파일의 재생성 가능성, 실행 중 프로세스 충돌, 삭제 전 SHA·크기·사유·복구 가능성 감사
- obsolete candidate의 reference·owner·successor 감사와 승인 evidence를 보존하는 binary cleanup 분류
- 저장소별 cleanup manifest와 SAFE-TRANSIENT·OWNER-CONFIRM·UNKNOWN-BLOCKED 상태 관리
- 실제 정적 URL·build·commit·manifest SHA·저장 schema에 연결된 사람 화면 테스트 카드와 원문 피드백 감사
- S0 전체, D1 첫 손님, D1 전체 영업·정산·저장·D2 전환의 사람 `PASS|조건부 PASS|FAIL` 재현 관리

### 제외

- gameplay·UI·시나리오·밸런스 기능 구현
- 아트 생성·수정·승인 판단
- `runtimeRegistrationAllowed` 수동 변경과 manifest 직접 편집
- 다른 역할의 active source 또는 tracked dirty 파일 삭제·stash·commit
- 배포 URL 개방과 외부 사용자 모집
- D2~D5 범위의 정리, 기능 코드 삭제 또는 리팩터링
- PM·QA가 사용자 시각 승인이나 사람 `PASS`를 대신 생성·추정하는 행위

## S0+D1 아티팩트 위생 안전 정책

- QA는 Playwright·Vitest·로컬 캡처 서버·asset promotion·art finalizer의 실행 여부를 먼저 확인한다.
  하나라도 실행 중이면 해당 process가 사용할 `test-results`, staging, receipt를 건드리지 않고 inventory만 갱신한다.
- `git ignored`이고 도구가 자동 재생성하며, live process·approval·provenance·handoff가 참조하지 않고
  사용자 source가 아닌 정확한 경로만 SAFE-TRANSIENT로 삭제할 수 있다. 와일드카드·저장소 루트 재귀 삭제는 금지한다.
- superseded/rejected binary는 상태 문자열만으로 삭제하지 않는다. 명시적 상태, 최신 사용자 승인 successor,
  manifest/finalizer/runtime-handoff/provenance 및 세 저장소 정본 참조 0건, Artist의 묶음 삭제 확인을 모두
  충족한 뒤에만 DELETE-APPROVED로 바꾼다. README·provenance·승인/반려 evidence와 유일 source는 보존한다.
- `app/assets` 심볼릭 링크, `app/public/assets/**`, manifest, promotion receipt/staging, LFS tmp,
  `node_modules`, reference image, 승인 review·GLB·texture, finalizer·handoff·provenance·completion·optimization
  report, `REJECTIONS.md`, 현재 후보와 D1 release definition/콘텐츠 정본은 자동 삭제 대상이 아니다.

## 작업 절차

1. 세 저장소의 branch·HEAD·dirty 경로와 역할별 소유자를 고정된 시각에 기록한다.
2. 현재 전체 verify 실패를 재현해 제품 결함·테스트 드리프트·환경 flaky로 분류한다.
3. 개발자 1 tray/6칸 계약, 개발자 2 그릴 UI 안전영역, Artist 1 master parity의 동일 bounds를 대조한다.
4. Artist 1의 그릴 tray·음식 handoff는 checkerboard·단독 승인이 아니라
   `CM-GRILL-STATION-QUEUED-SELECTION R3` 실제 위치의 FHD/720 원근·톤 승인 증빙이
   있는지 확인하고, 음식 없는 tray R2 승인이 음식 검수보다 먼저인지 감사한다.
5. Artist 1·2·3 handoff의 승인·재조립·finalizer 상태와 공개 placeholder를 owner별로 집계한다.
6. 역할별 수정이 끝난 뒤 충돌 없는 통합 순서와 최소 회귀 묶음을 실행한다.
7. 최초 공개 gate의 통과·차단·남은 위험을 재현 명령과 증빙 경로로 보고한다.
8. cleanup 전후에 assets/reference/pipeline/runtime binding/placeholder/manifest SHA/D1 정적 URL/FHD·720
   evidence가 보존됐는지 감사하고, 실행 중 병행 작업이 있으면 전체 verify 결과를 확정하지 않는다.
9. PM이 고정한 실제 URL/build로 `template/사람_화면_플레이테스트_피드백_템플릿.md` 테스트 카드를
   만들고, 테스터 원문 판정과 캡처·진단 경로를 stage epic에 연결한다.
10. `조건부 PASS`·`FAIL`은 재현 가능한 결함으로 분류해 원래 owner에게 돌리고 재검수 전 공개를 차단한다.

## 의존성과 인계 조건

- 병행 작업: 개발자 1 작업 005·009, 개발자 2 작업 003·005, 개발자 3 작업 010,
  Artist 1 작업 000, Artist 2 작업 023, Artist 3 작업 025
- 후속 작업: 개발자 2 작업 006
- 각 결함의 수정은 원래 소유 역할이 수행하고 통합 QA·릴리스는 재현·분류·재검증을 소유한다.
- 다른 역할이 수정 중인 active source·tracked dirty 파일에는 write하지 않는다. 단, 위 안전 정책을 모두
  충족한 ignored transient는 QA가 cleanup manifest의 정확한 경로에 한해 정리할 수 있다.

## 완료 기준

- [ ] 역할별 dirty 변경과 통합 순서가 파일 단위로 기록된다.
- [ ] 전체 Vitest와 Chromium FHD/720 회귀가 반복 실행에서 통과하고 flaky가 별도 기록된다.
- [ ] Chrome·Edge FHD/720 수동 smoke 결과가 기록된다.
- [ ] S0·D1 placeholder·에셋 404·처리되지 않은 console 오류가 0개다.
- [ ] S0 프롤로그 회귀가 `S0-KEY-SELECT → S0-GATE-OPEN` 2클릭(KEY→GATE)과 점화 story dialogue를 필수 runtime·입력 회귀로 포함한다. (2026-08-02 갱신: `S0-STATE-CHARCOAL`/`S0-CHARCOAL-IGNITE` phase와 `ST-S0-BRAZIER`/`PR-CHARCOAL-IGNITION` 아트는 점화 대사 대체로 deprecated이므로 placeholder·runtime 회귀에서 요구하지 않는다.)
- [ ] 승인 없는 runtime handoff·중복 `semanticOwner`·부분 manifest 승격이 자동 차단된다.
- [ ] station FHD/720 소비 승인 없는 그릴 tray·음식 finalizer handoff가 차단된다.
- [ ] 차단·높은 등급 결함이 0개이고 남은 중간·낮은 결함에 소유자와 재현 절차가 있다.
- [ ] cleanup manifest에 삭제 전후 SHA·크기·사유·복구 방법과 live-process 충돌 여부가 기록된다.
- [ ] 승인 evidence·runtime provenance·handoff·현재 후보 손실 0건을 증명한다.
- [ ] S0 전체, D1 첫 손님과 D1 전체 영업의 사람 화면 피드백 원문·환경·build가 기록된다.
- [ ] 사람 `PASS` 전 stage 완료와 공개 PASS를 선언하지 않는다.

## 구현 및 검증 결과

- app 구현 위치: read-only audit만 수행, 소스 변경 없음
- 구현 기준 spec 버전: 위 참조 spec
- 구현 기준 태스크 버전: `v1.3.1`
- 검증 방법: 저장소 상태·cleanup manifest 감사, 병행 process 확인, assets/reference/pipeline/runtime
  inventory, static URL, Chrome·Edge FHD/720 smoke
- 현재 runtime 기준: runtime asset `12`, D1 required `44`, 승인·binding `9`, placeholder `35`,
  drink placeholder `5`.
  `BG-WORKSPACE-DRINK@R2-B1` promotion과 `drink.scene` binding은 완료됐으며 두 D1 진입점의 해당
  missing ID는 제거됐다.
- 2026-08-01 재감사: `npm run assets:validate` `12/12`,
  `npm run visual:references:validate` `36/36`. runtime inventory는 required `44`, binding `9`,
  placeholder `35`, drink placeholder `5`, unbound approved `0`이고 manifest SHA-256은
  `3ef18fffe69b4d447a00c1ecb99bdaf22c7e6a0848f242e3a580acf60bab13c7`다.
- art pipeline v8은 최초 legacy provenance `23`건을 보고했다. Artist 1이 승인 의미를
  바꾸지 않고 provenance `14`건과 기존 승인 evidence 기반 report bridge `6`건을 정합한 뒤
  PM 독립 실행은 contract `YS-ASSET-PIPELINE-v8`, report `25`, provenance `38`, error `3`이다.
  남은 오류는 `assembly/r1`, `finished-tray/r1`, `grill/master/r3`의 증거·의미 결정 blocker다.
- Developer 2 작업 013은 `TEX-BEER-LIQUID`, `VFX-BEER-CORE`를 required로 집계하고
  `FD-BEER-SERVED`를 기존 손님 composition에서 파생되는 visual로 판정했다. 일부 required visual을
  제외해도 placeholder 0을 반환하지 않는 contract test가 통과하며 QA 전체 재감사는 남았다.
- P0 선택 회귀: assets `12/12`, reference `36/36`, S0/D1 unit `19/19`,
  Chromium FHD/720 최소 E2E `20/20` 통과. 전체 verify 결과는 아니므로 과거 전체 수치는 stale다.
- D1 task 011 QA R3: dirty 6파일 SHA 일치, 구형 버튼 문구 0건, task unit `6/6`,
  Chromium FHD/720 E2E `24/24`, 최초 실패·retry·40404 `0`으로 GREEN이다.
- 현재 정적 공개 URL/build가 없고 S0 placeholder, D1 최소 36 누락, 미승인 후보와 pipeline 실패가
  남아 사람 테스트 카드는 아직 발행할 수 없다.
- 테스트 상태: 과거 Vitest·Playwright 결과는 현재 결과가 아니라 **stale**이다. 이번 hygiene 감사 시점에는
  다른 역할의 `npm run verify`가 실행 중이므로 전체 결과를 확정하지 않는다.
- 공개 준비 상태: Developer 1은 정적 D1 release artifact 입력 대기, Developer 2는 tray R6 DOM reserved
  계약 정합화, Developer 3은 실제 정적 release artifact 제작, Artist 1은 raw 네기마 station 소비 검수,
  Artist 3은 `ST-DRINK-BEER-TIER-1` 제작 재개를 각각 소유한다.
- 남은 위험: 세 저장소의 병렬 dirty 변경과 placeholder 35개, art pipeline 3건, Artist finalizer 잔여 항목, Chromium/Chrome/Edge
  FHD·720, static host와 사람 화면 피드백의 최신 증빙 부재가 공개를 차단한다. cleanup과 자동 테스트는
  사람 검수를 대체하지 않는다.
