# docs 저장소 AI 작업 지침

## 저장소 역할

이 저장소는 아이디어, 요구사항, 작업 계획과 AI 지침만 관리한다. 게임 개발 및 운영에 사용되는 소스코드, 실행 스크립트, 빌드 설정, 배포 설정은 이 저장소에 만들지 않는다.

구현과 운영 변경은 반드시 별도 `app` 저장소에서 수행한다.
콘셉트·편집 원본·검수판·provenance·캡처 증빙은 두 Git 저장소 밖의
형제 디렉터리 `../art-workspace`에서 관리한다. `app`에는 승인·자동 검증을 통과한
최종 runtime 에셋만 `public/assets`에 둔다.

## 사용자 요청 유형 구분

- 사용자가 `아이디어`, `초안`, `시나리오 구상`, `기획안 작성`처럼 원본 자료 작성을 요청하면 결과는 `inbox/`의 새 파일로만 작성한다.
- 위 요청만으로는 `spec/`, `spec/meta/`, `epic/`을 생성·수정하거나 변경사항 반영 파이프라인을 실행하지 않는다.
- 사용자가 `spec 반영`, `spec 작성`, `epic 작성`, `작업 계획 반영`, `변경사항 반영 파이프라인 실행`처럼 대상 영역을 명시적으로 지시한 경우에만 해당 영역을 변경한다.
- `inbox/` 내용을 읽거나 파악하라는 요청은 분석 범위를 뜻할 뿐, `spec/`·`epic/` 갱신 승인으로 해석하지 않는다.
- 요청의 산출물 위치가 불명확하면 `inbox/` 초안 작성을 기본값으로 삼고 `spec/`·`epic/`은 변경하지 않는다.
- 사용자가 AI에게 `inbox/` 초안 생성을 명시적으로 요청한 경우에는 새 파일을 만들 수 있다. 이때 기존 사람 원본은 수정하지 않고 AI 생성 초안임을 파일에 표시한다.

## 문서 처리 흐름

1. 사람이 새 아이디어, 개발 중 발견한 개선사항과 수정사항을 정제하지 않은 원본 그대로 `inbox/`에 기록한다.
2. AI는 `inbox/`를 읽기 전용 원본으로 분석하고 관련 `spec/` 요구사항 상세 설계서를 갱신한다.
3. 갱신된 요구사항이 기존 `epic/` 작업에 미치는 영향을 확인한다.
4. 상태가 `대기` 또는 `진행 중`인 작업에는 변경사항을 즉시 반영한다.
5. 기존 작업에 포함하기 어려운 범위는 담당 개발자와 순번을 정해 새 작업으로 생성한다.
6. `epic/작업_현황.md`에 각 개발자의 현재·대기·보류·완료 작업을 동기화한다.
7. 구현은 갱신된 `spec/`과 `epic/`을 근거로 `app` 저장소에서만 수행한다.

세부 절차는 `template/AI_변경사항_반영_파이프라인.md`를 따른다.

## 공통 원칙

- 모든 가이드, 예제, 문서 본문과 커밋 메시지는 한국어로 작성한다.
- `inbox/`의 기존 사람 입력 데이터는 AI가 정리하거나 덮어쓰지 않는다. AI 생성 초안은 사용자의 명시적 요청이 있을 때만 별도 새 파일로 추가한다.
- `inbox/` 내용만을 근거로 구현을 시작하지 않는다.
- `spec/` 변경에는 근거가 된 `inbox/` 문서와 변경 이력을 남긴다.
- `epic/` 작업은 관련 요구사항 식별자와 문서 경로를 참조한다.
- 새 `spec/`·`epic/` 문서는 `v1.0.0`으로 시작하고, 기존 문서를 변경하면 문서 버전과 변경 이력을 함께 갱신한다.
- 버전 판단은 `template/spec_epic_문서_버전_관리_지침.md`를 따른다.
- 완료된 작업은 과거 기록으로 보존하고 직접 고치지 않는다. 후속 변경은 새 작업으로 만든다.
- 불명확하거나 서로 충돌하는 입력은 임의로 확정하지 않고 미해결 질문으로 남긴다.

## 금지 사항

- `docs` 저장소에 애플리케이션 소스코드 추가
- `docs` 저장소에 실행 가능한 운영·배포 자동화 추가
- AI가 `inbox/`의 사람 입력 데이터를 수정·이동·삭제하거나 정제된 문서로 교체
- `inbox/` 메모를 검토 없이 요구사항 또는 작업으로 확정
- 진행 상태와 영향 분석 없이 기존 작업 범위 변경
- 완료된 작업 문서를 현재 작업처럼 다시 열어 수정
